# 团队试用版登录、权限与上线需求文档 v0.1

## 1. 定位

本需求面向“项目资料库 + 项目执行工作台”的团队试用阶段。

系统从单人本地使用升级为多人协作后，需要先解决四件事：

```text
谁在使用？
他能看到什么？
他能改什么？
关键修改是否形成闭环和记录？
```

第一版目标不是做完整企业级 SSO 或复杂审批平台，而是让项目经理、工程师和管理者可以在公司内部先用起来，并保证文件、任务、风险和 Due Date 的流转可追踪。

## 2. 已确认业务约定

- 企业邮箱体系：阿里企业邮箱。
- 允许登录邮箱域名：`jinxiangsz.com`。
- 初始管理员邮箱：`rongkai@jinxiangsz.com`。
- 登录方式第一版采用 SMTP 邮箱验证码。
- 角色拆分为：`Admin`、`PM`、`Engineer`。
- 同一个用户可以拥有多个角色，例如初始管理员可以同时是 `Admin + PM`。
- 所有人原则上可以查看全部项目，但默认页面应按角色过滤重点信息，避免工程师被全量项目淹没。
- 工程师可以创建风险。
- 工程师可以申请修改 Due Date，但必须填写理由，且 PM 确认后才生效。
- PM 可以修改项目资料库信息。
- 只有 Admin 可以删除项目和文件记录。
- 上线运行环境待确认。

## 3. 设计原则

### 3.1 身份可信

企业邮箱作为用户唯一身份。

系统只接受 `@jinxiangsz.com` 邮箱登录，避免外部邮箱误入。

### 3.2 权限后端强制

前端可以隐藏按钮，但最终权限必须由后端 API 校验。

例如工程师即使构造请求，也不能直接确认交付物或删除项目。

### 3.3 默认减负

所有人可以查看全部项目，不代表默认把全部项目堆在首页。

工程师默认看自己的任务、参与项目、自己创建或负责的风险、返工任务。

PM 默认看待确认文件、逾期任务、Due Date 变更申请、高风险和阻塞。

### 3.4 关键动作留痕

所有会影响项目执行状态的动作都必须记录：

- 谁操作
- 操作时间
- 操作对象
- 操作前状态
- 操作后状态
- 理由或备注

## 4. 角色与权限

| 操作 | Engineer | PM | Admin |
|---|---:|---:|---:|
| 查看全部项目 | 是 | 是 | 是 |
| 查看工程可见文件 | 是 | 是 | 是 |
| 查看客户报价/PO | 否 | 是 | 是 |
| 查看工作台 | 是 | 是 | 否，除非同时具备 PM/Engineer |
| 创建任务 | 否/待定 | 是 | 否，除非同时具备 PM |
| 更新自己负责的任务状态 | 是 | 是 | 否，除非同时具备 PM/Engineer |
| 上传任务交付文件 | 是 | 是 | 否，除非同时具备 PM/Engineer |
| 确认/驳回交付文件 | 否 | 是 | 否，除非同时具备 PM |
| 创建风险/问题 | 是 | 是 | 否，除非同时具备 PM/Engineer |
| 关闭风险/问题 | 否/待定 | 是 | 否，除非同时具备 PM |
| 申请修改 Due Date | 是 | 是 | 否，除非同时具备 PM/Engineer |
| 直接生效修改 Due Date | 否 | 是，但必须写理由 | 否，除非同时具备 PM |
| 确认/驳回 Due Date 变更申请 | 否 | 是 | 否，除非同时具备 PM |
| 修改项目资料库信息 | 否 | 是 | 否，除非同时具备 PM |
| 删除项目记录 | 否 | 否 | 是 |
| 删除文件记录 | 否 | 否 | 是 |
| 用户管理 | 否 | 否 | 是 |
| 系统设置/备份配置 | 否 | 否 | 是 |

说明：

- `PM` 和 `Admin` 不建议合并。可以由同一个人同时拥有两个角色，但权限定义保持分开。
- 当前不设置独立 `Readonly` 角色；管理层或临时查看需求先通过 PM 或后续看板权限讨论处理。
- “创建任务”是否允许工程师操作后续再定。第一版建议由 PM 分配任务，工程师可以补充备注、提交文件、创建风险。
- 文件可见度先由系统内文件索引过滤，最终共享目录权限需要等 NAS 试用时用 ACL 落地。

## 5. 登录需求

### 5.1 登录入口

登录页字段：

- 企业邮箱
- 验证码
- 发送验证码按钮
- 登录按钮

登录流程：

```text
输入企业邮箱
→ 校验邮箱域名是否为 @jinxiangsz.com
→ 发送 6 位数字验证码
→ 用户输入验证码
→ 验证成功后创建登录会话
→ 根据角色进入对应默认视图
```

### 5.2 验证码规则

建议默认规则：

- 6 位数字验证码。
- 10 分钟有效。
- 同一邮箱 60 秒内不能重复发送。
- 同一验证码连续输错 5 次后失效。
- 验证码使用后立即失效。
- 验证码不明文长期保存，数据库中建议保存哈希值和过期时间。

### 5.3 用户首次登录策略

由于第一批名单暂时无法完整提供，第一版建议：

```text
允许 @jinxiangsz.com 用户登录
→ 如用户不存在，自动创建用户
→ 状态为 pending，暂不能登录
→ Admin 后续在用户管理中调整为 PM / Engineer / Admin
→ Admin 启用后用户才能登录
```

初始用户：

```text
邮箱：rongkai@jinxiangsz.com
角色：Admin + PM
状态：启用
```

### 5.4 SMTP 配置

SMTP 信息不写死在代码中，应放在配置文件或环境变量中。

需要支持：

- SMTP host
- SMTP port
- SSL/TLS 类型
- 发信邮箱
- 发信名称
- SMTP 授权码或密码

待确认项：

- 是否开通系统发信邮箱，例如 `project-system@jinxiangsz.com`。
- 阿里企业邮箱 SMTP 授权方式。
- 是否允许系统邮箱向公司内部邮箱发送验证码。

## 6. Due Date 修改与审批

### 6.1 核心规则

Due Date 是项目执行闭环中的关键字段，不能无理由随意改。

第一版规则：

```text
任何 Due Date 修改都必须填写理由。
Engineer 修改 Due Date 只是提交申请。
PM/Admin 确认后，新的 Due Date 才正式生效。
PM/Admin 自己直接修改 Due Date 时，也必须填写理由并写入日志。
```

### 6.2 工程师申请修改 Due Date

入口：

- 我的待办任务卡片
- 项目执行详情中的任务行

工程师点击“申请改期”后，需要填写：

- 原 Due Date，系统自动带出。
- 新 Due Date，必填。
- 修改理由，必填。
- 影响说明，可选。

理由示例：

- 客户资料未提供，无法继续设计。
- 供应商交期延后。
- 设计方案变更，需要重新评估。
- 现场安装窗口调整。
- 任务范围增加。

提交后状态：

```text
Due Date 仍保持原日期
→ 创建 Due Date 变更申请
→ 申请状态为 Pending
→ PM 我的待办出现“待确认改期”
→ 任务卡片显示“改期待确认”
→ 执行日志记录申请动作
```

### 6.3 PM 确认或驳回

PM 在“我的待办”中看到改期申请。

PM 可以：

- 确认改期
- 驳回改期
- 修改为另一个 Due Date 后确认

PM 确认时需要记录：

- 确认人
- 确认时间
- 最终 Due Date
- PM 备注，可选

确认后：

```text
任务 Due Date 更新为最终日期
→ 改期申请状态变为 Approved
→ 工程师收到系统内通知
→ 项目/任务执行日志记录改期生效
```

PM 驳回时必须填写驳回理由。

驳回后：

```text
任务 Due Date 不变
→ 改期申请状态变为 Rejected
→ 工程师收到系统内通知
→ 任务保留原 Due Date
→ 执行日志记录驳回原因
```

### 6.4 PM/Admin 直接修改 Due Date

PM/Admin 可以直接修改 Due Date，但必须填写理由。

直接修改时系统应自动生成一条已批准的改期记录：

```text
旧 Due Date
新 Due Date
修改理由
操作人
操作时间
审批状态：Approved
```

这样即使 PM 直接改，也能回溯为什么改。

### 6.5 多次改期

同一任务可能多次改期。

系统需要保留完整历史，不覆盖旧记录。

任务详情中应展示：

- 当前正式 Due Date
- 是否存在待审批改期
- 历史改期记录

如果同一任务已有 Pending 改期申请，原则上不允许再次提交新的 Pending 申请，除非先撤回或由 PM 处理。

## 7. 通知需求

第一版先做系统内通知，不依赖邮件通知。

### 7.1 PM 通知

PM 默认待办包含：

- 待确认交付文件
- 待确认 Due Date 变更
- 超期任务
- 高风险/阻塞

### 7.2 工程师通知

工程师默认待办包含：

- 我的未完成任务
- 我的返工任务
- 我的改期申请结果
- 与我相关的风险状态变化

### 7.3 邮件通知

SMTP 第一版主要用于验证码登录。

邮件通知可以作为后续增强：

- Due Date 改期申请发送给 PM。
- PM 确认/驳回结果发送给工程师。
- 高风险或严重超期发送提醒。

## 8. 用户管理需求

Admin 用户管理页第一版需要支持：

- 查看用户列表。
- 按邮箱、姓名、角色、状态筛选。
- 修改用户姓名。
- 设置用户角色，可多选。
- 启用/停用用户。
- 查看最后登录时间。

用户字段建议：

- 邮箱
- 姓名
- 角色列表
- 状态：启用/停用
- 创建时间
- 最后登录时间

停用用户后：

- 不能再登录。
- 历史操作记录仍保留该用户邮箱和姓名。

## 9. 数据模型草案

### 9.1 users

用户主表。

建议字段：

- id
- email
- display_name
- status
- created_at
- updated_at
- last_login_at

### 9.2 user_roles

用户角色表。

建议字段：

- user_id
- role_code

角色值：

```text
admin
pm
engineer
```

### 9.3 login_codes

邮箱验证码表。

建议字段：

- id
- email
- code_hash
- expires_at
- attempt_count
- used_at
- created_at

### 9.4 sessions

登录会话表。

建议字段：

- id
- user_id
- session_token_hash
- expires_at
- created_at
- last_seen_at

### 9.5 notifications

系统内通知表。

建议字段：

- id
- user_id
- type
- title
- body
- related_type
- related_id
- status
- created_at
- read_at

### 9.6 due_date_change_requests

Due Date 变更申请表。

建议字段：

- id
- task_id
- project_id
- old_due_date
- proposed_due_date
- final_due_date
- reason
- impact_note
- status
- requested_by
- requested_at
- reviewed_by
- reviewed_at
- review_note
- created_at
- updated_at

状态值：

```text
pending
approved
rejected
cancelled
```

## 10. 页面与入口

### 10.1 登录页

未登录用户访问系统时进入登录页。

登录成功后按角色进入默认视图：

- PM：项目执行工作台。
- Engineer：我的待办。
- Admin：用户管理或系统设置；如同时具备 PM，则可进入 PM 工作台。

### 10.2 我的待办

根据登录用户和角色自动显示内容，不再依赖手动输入负责人。

工程师看到：

- 我的任务
- 我的返工
- 我的风险
- 我的改期申请结果

PM 看到：

- 待确认交付文件
- 待确认 Due Date 变更
- 超期任务
- 高风险/阻塞

### 10.3 用户管理

仅 Admin 可见。

用于角色分配、用户停用和查看登录情况。

### 10.4 Due Date 变更窗口

任务行中提供“申请改期/修改 Due Date”入口。

窗口字段：

- 当前 Due Date
- 新 Due Date
- 修改理由，必填
- 影响说明，可选

PM 审批窗口字段：

- 申请人
- 原 Due Date
- 申请 Due Date
- 申请理由
- 影响说明
- 最终 Due Date
- 审批备注
- 确认/驳回按钮

## 11. 上线试用建议

第一阶段建议部署为公司内网 Web 系统。

候选方式：

- 固定 Windows 主机。
- 小型服务器。
- NAS 旁边的小主机。

基础要求：

- 机器长期在线。
- 团队电脑可以通过局域网访问。
- 数据库和客户资料目录定期备份。
- 服务可随 Windows 启动自动启动。
- SMTP 配置不进入 Git。

第一批试用建议：

- 1 个 PM/Admin。
- 2 到 3 个工程师。
- 选择 1 到 2 个真实新项目跑完整流程。

## 12. 第一版验收标准

登录与用户：

- 只有 `@jinxiangsz.com` 邮箱可以请求验证码。
- 验证码有效期、重发间隔、错误次数限制生效。
- `rongkai@jinxiangsz.com` 初始化为 `Admin + PM`。
- 新用户首次请求验证码后为待分配状态，Admin 分配角色并启用前不能登录。
- Admin 可以调整用户角色。

权限：

- 工程师不能确认交付物。
- 工程师不能删除项目或文件记录。
- PM 可以确认/驳回交付物。
- PM 可以修改项目库信息。
- Admin 可以管理用户和删除记录。

Due Date：

- 工程师申请改期必须填写理由。
- 工程师申请改期后，原 Due Date 不立即改变。
- PM 待办能看到待确认改期。
- PM 确认后，新 Due Date 才生效。
- PM 驳回时必须填写理由。
- 所有 Due Date 修改都有历史记录。

通知：

- 改期申请产生 PM 系统内通知。
- 改期确认/驳回产生工程师系统内通知。
- 通知可读、可标记已读。

审计：

- 登录、交付文件确认、Due Date 修改、项目资料修改、删除动作均有日志。

## 13. 暂未确认事项

- SMTP 系统发信邮箱。
- SMTP host、port、SSL/TLS 和授权码。
- 系统最终运行机器。
- 项目资料目录是否迁移到 NAS。
- 数据库备份位置和周期。
- 工程师是否允许创建任务。
- 工程师是否允许关闭自己创建的风险。
- 是否需要邮件通知，还是第一版仅系统内通知。
