# 项目执行工作台 Gap 与下一步计划 v0.1

## 当前判断

之前提到的前三项：

```text
1. 风险分层
2. 新增任务窗口重做
3. 工作台页面信息减负
```

现在都已经做出了第一版，不再是“从零开始”的下一步。

当前 gap 是：这些功能已经具备入口和基本逻辑，但还没有完全变成工程师和项目经理日常愿意使用的稳定工作流。

## 已完成到什么程度

### 风险分层

已完成：

- 风险/问题已经独立成窗口。
- 支持产品/产线级、设备/WO级、任务级范围。
- 任务级风险必须关联具体任务。
- 产品/产线级风险可以在同一项目群下的多个设备中显示。
- 风险可以创建、修改、删除、关闭，并写入执行日志。

当前 gap：

- 还没有跨项目风险总览页。
- 阻塞任务已经支持自动创建风险或关联已有风险，但前端引导仍可继续打磨。
- 产品级风险在页面上的来源说明还可以更清楚，避免工程师误以为是当前单台设备独有问题。

下一步做法：

- 增加跨项目风险列表，优先显示打开、高严重度、临近 due date 的风险。
- 在任务状态改为“等待资料”或“阻塞”时，提示是否同步生成风险。
- 在项目执行页中把产品级风险标识为“产品/产线共享风险”，并显示来源设备。

### 新增任务窗口

已完成：

- 新增任务已经改为弹窗。
- 手动添加在上层。
- 模板通过选择后再显示对应任务条目。
- 支持勾选模板条目后批量添加任务。
- 任务可以删除、修改、上传交付物。

当前 gap：

- 模板条目的交互仍偏粗糙，信息密度和视觉比例还需要打磨。
- 模板任务只是静态条目，还不能根据项目性质、INQ/WO状态、是否夹具/改造自动推荐。
- 批量添加后缺少二次整理能力，例如一次性设置负责人、due date 偏移、是否需要文件。

下一步做法：

- 重做模板条目的视觉结构：左侧复选，中央任务标题/工作包，右侧 due date/是否需要文件，避免大块空白。
- 根据项目性质和是否已有 WO 号推荐默认模板。
- 批量添加时支持统一负责人和默认 due date 偏移。

### 工作台页面信息减负

已完成：

- 工作台可以从资料库详情打开新窗口。
- 项目执行模式隐藏资料库主表头。
- 右侧项目信息已经压缩为项目列表卡片。
- 执行日志已经改为隐藏式侧边栏。
- 新增任务、风险/异常、执行日志都变成按钮入口。
- PM 才显示待确认交付物区域的临时角色逻辑已预留。

当前 gap：

- 项目列表和项目详情之间的信息优先级还需要再校准。
- 缺少“我的任务”视角，工程师仍然需要先进项目再找任务。
- PM 的待确认交付物只是局部区域，还没有变成集中处理入口。

下一步做法：

- 增加工作台视图切换：项目视图、我的任务、风险、待确认。
- 项目列表只保留编号、客户/产品、当前负责人、下一步、due date、风险标记。
- 工程师视角优先显示“我负责且未完成”的任务。

## 现在真正的下一阶段

建议下一阶段不再继续堆入口，而是做“工作流闭环”：

```text
任务状态变化
→ 是否需要文件
→ 文件上传归档
→ PM确认/驳回
→ 任务关闭或返工
→ 风险/阻塞同步
→ 项目列表摘要更新
```

优先级：

1. 我的任务视图：降低工程师使用负担。
2. 跨项目风险视图：让 PM 能抓住真正卡住的事。
3. 待确认交付物视图：让文件确认形成闭环。
4. 任务阻塞到风险的联动：让风险不靠人事后补。
5. 模板批量添加体验打磨：让新项目开工更快。

## 暂不做

- 完整账号权限系统。
- 甘特图。
- 自动排产。
- ERP/邮件/微信深度集成。
- 复杂工作流引擎。

这些可以等“任务、风险、交付物确认”三条主线稳定后再做。

## 2026-06-05 工作台闭环需求确认

### 已确认规则

- 任务由 PM 创建和分配，第一版不开放工程师创建任务。
- 所有任务关闭都需要 PM 确认。
- 需要交付文件的任务，工程师上传文件后进入“已提交/待确认”，PM 确认后才关闭。
- PM 驳回交付文件后，工程师必须重新提交文件，不能只改说明后直接关闭。
- 风险关闭只允许 PM 操作。
- 任务标记为阻塞时，应关联风险；第一版至少要求填写阻塞原因，并提示/引导创建或关联风险。
- “我的任务”第一版兼容负责人姓名匹配；后续登录账号稳定后，再逐步切换为用户账号绑定。
- 项目资料库列表先显示简化执行摘要，摘要字段后续根据前端信息密度再调整。

### 不需要交付文件的任务边界

第一版建议把任务分为两类：

```text
需要文件：方案、报价输入、图纸、BOM、程序、调试记录、验收资料、发货资料等。
不需要文件：沟通确认、内部评审、客户资料跟进、会议动作、风险处理、采购状态跟进等。
```

不需要文件的任务也不能由工程师直接关闭，推荐闭环为：

```text
工程师提交完成说明
→ PM 查看说明
→ PM 确认关闭或驳回返工
```

### 第一版最小闭环

```text
PM 创建任务
→ 工程师在我的任务中处理
→ 需要文件则上传文件，不需要文件则提交完成说明
→ 阻塞任务必须关联或生成风险
→ PM 确认/驳回
→ 任务关闭或返工
→ 项目库显示简化执行摘要
```

### 项目库摘要建议字段

项目资料库列表中只显示轻量摘要，不展开工作台细节：

```text
执行进度
逾期任务数
阻塞/高风险标记
待确认文件数
下一到期日期
当前主要负责人
```

## 2026-06-05 第一版闭环实现记录

### 已实现

- 新增无文件任务完成说明提交流程：

```text
工程师/PM 提交完成说明
→ 任务进入已提交
→ PM 待办显示待确认完成说明
→ PM 确认后任务关闭
→ PM 驳回后任务进入返工
```

- PM 自己负责的无文件任务支持一键提交并确认：

```text
PM 填写完成说明
→ 直接确认关闭
→ 不再进入“待确认完成说明”队列
```

- 需要文件的任务继续走交付物流程：

```text
上传文件
→ 自动归档资料库
→ PM 待确认
→ PM 确认关闭任务
→ PM 驳回后任务返工
→ 必须重新提交新文件，不能直接确认旧的驳回文件
```

- 普通任务编辑不能绕过闭环：
  - 不能直接把无文件任务改成已完成。
  - 不能直接把需要文件的任务改成已提交/已完成。
  - 普通状态下拉只保留可人工维护状态；“已提交/已确认/已完成”由提交、确认、交付物流程自动生成。
- 阻塞任务必须填写阻塞原因。
- 阻塞任务可以选择已有打开风险进行关联；未选择时自动生成任务级高风险。
- PM 待办新增“待确认完成说明”。
- 完成说明确认/驳回已经从 `prompt` 改为统一弹窗。
- 项目资料库列表新增“执行”摘要列：
  - 任务进度。
  - 超期任务。
  - 阻塞任务。
  - 待确认任务。
  - 高风险。
  - 下一动作和 Due Date。
- 前端错误提示兼容 FastAPI `detail`，避免只显示“请求失败”。

### 已验证

- 后端测试覆盖：
  - 无文件任务不能直接关闭，必须提交完成说明并由 PM 确认。
  - 阻塞任务自动生成任务级风险。
  - 交付物驳回后不能直接确认旧文件，必须重新提交。
- 浏览器冒烟：
  - 工作台页面可打开。
  - 登录后可进入工作台。
  - 未发现工作台运行时脚本错误。

### 剩余 gap

- PM 自己负责的文件型任务仍走上传交付物再确认，后续可评估是否允许 PM 上传后直接确认。
- 阻塞任务关联已有风险的界面还只是下拉选择，后续可做成更清晰的风险选择窗口。
- 项目库执行摘要已经显示，但字段和视觉密度还需要根据真实项目数据继续调整。
- “我的任务”仍兼容负责人姓名匹配，尚未完全绑定登录账号。
- 跨项目风险总览仍未实现。
