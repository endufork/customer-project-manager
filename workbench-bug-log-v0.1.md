# 工作台 Bug 记录 v0.1

本文档用于记录工程执行工作台每次发现的 bug、根因、修复方式和回归验证。后续每次出现工作台相关 bug，都应追加记录。

## 记录规则

- 每个 bug 单独编号。
- 必须记录：发现日期、现象、影响范围、根因、修复、验证、剩余风险。
- 如果 bug 涉及按钮、状态流转、文件归档、Due Date、风险、权限，应同步检查同类入口。
- 修复后应尽量补自动化测试；前端交互 bug 应尽量用 Playwright 或浏览器验证。

## BUG-WB-001：任务编辑保存后 Due Date 被清空

发现日期：2026-06-06

现象：

- 新建任务填写 Due Date 后，再次编辑并保存任务，Due Date 被清空。
- 再次进入同一任务编辑时，Due Date 显示为空。

影响范围：

- 工程工作台任务编辑。
- 我的任务和项目执行页中的任务 Due Date 展示。

根因：

- 任务编辑表单中的 Due Date 是只读显示字段，不应随普通保存提交。
- 后端 PATCH 早期按完整模型处理，缺失字段会被当成空值覆盖旧值。
- 前端日期显示没有统一规范化，遇到字段来源或格式变化时容易显示为空。

修复：

- 后端任务 PATCH 改为部分更新，未提交字段保留原值。
- 普通任务保存不再直接修改 Due Date，Due Date 修改继续走改期窗口和理由记录。
- 前端统一使用 `workbenchDateValue()` 规范化 Due Date 显示。
- 补回归测试：PATCH 任务不传 Due Date 时，原 Due Date 必须保留。

验证：

- `tests/test_workbench_api.py` 覆盖。
- `.\tools\check.cmd` 通过。
- 临时数据库 Playwright 验证：保存任务后 Due Date 保持不变。

剩余风险：

- 如果实际数据库中已有异常格式日期，需要后续数据清洗或前端提示。

## BUG-WB-002：任务状态可手动切到“已提交”，导致流程混乱

发现日期：2026-06-06

现象：

- 用户在任务编辑下拉框选择“已提交”后，页面显示“完成说明已提交”，但实际上没有通过提交按钮填写完成说明。
- 逻辑上用户不应手动选择“已提交/已确认/已完成”。

影响范围：

- 任务编辑表单。
- 无文件任务完成说明闭环。
- 文件型任务交付物闭环。

根因：

- 工作台任务状态全集直接用于普通编辑下拉框。
- 流程状态和人工维护状态没有在前端入口上区分。

修复：

- 普通状态下拉只保留可人工维护状态：

```text
未开始
进行中
等待资料
阻塞
需返工
已取消
```

- “已提交/已确认/已完成”由提交、上传、确认、驳回流程自动生成。
- 已提交/已确认/已完成任务在编辑区显示只读状态说明。
- 后端继续禁止普通 PATCH 绕过闭环。

验证：

- `tests/test_workbench_api.py` 覆盖普通 PATCH 不能直接改为 `submitted`。
- 临时数据库 Playwright 验证：已提交任务不再显示状态下拉。

剩余风险：

- 后续如果增加新任务状态，需要明确归类为“人工状态”或“流程状态”。

## BUG-WB-003：完成说明确认/驳回出现 Not Found

发现日期：2026-06-06

现象：

- 点击“提交并确认”“确认通过”或“确认驳回”时，页面弹出 `Not found`。
- 该问题表现为多按钮同类问题，疑似空 ID 或过期 DOM 触发 API。

影响范围：

- 无文件任务提交并确认。
- PM 确认完成说明。
- PM 驳回完成说明。
- 同类风险：交付物、风险、Due Date、打开项目等依赖按钮 `data-*` 的入口。

根因：

- 前端按钮依赖 `data-task-id`、`data-project-id`、`data-deliverable-id` 等字段。
- 某些情况下如果 DOM 过期、隐藏表单未正确写入 ID、事件重复绑定或按钮缺少 ID，会向 API 发出空路径，最终表现为 `Not Found`。
- 完成说明弹窗原来依赖隐藏 input 读 task id，不够稳。

修复：

- 新增 `requireDataset(button, key, label)`，所有关键按钮调用前先校验 ID。
- 完成说明弹窗同时写入 dialog dataset、form dataset 和 hidden input，并以 dataset 为优先来源。
- 确认/驳回接口返回 `project_id`，前端按返回项目刷新，避免使用过期项目 ID。
- 对任务、完成说明、交付物、风险、Due Date、打开项目按钮增加 ID 校验。
- 如果 ID 缺失，前端显示中文错误，不再请求空 API。

验证：

- 临时数据库 Playwright 验证：
  - 确认完成说明后状态为 `confirmed`。
  - 驳回完成说明后状态为 `rework`。
  - 页面不再出现 `Not found`。
  - 控制台无相关错误。

剩余风险：

- 仍需继续扩展按钮自动化巡检，覆盖更多实际数据状态。

## BUG-WB-004：完成说明确认弹窗显示驳回原因，界面误导

发现日期：2026-06-06

现象：

- 点击“确认完成说明”时，弹窗仍显示“驳回原因”输入框。
- 弹窗顶部的流程块视觉上像按钮，造成误解。

影响范围：

- PM 确认完成说明。
- PM 驳回完成说明。

根因：

- 全局 `label { display: grid; }` 覆盖了浏览器默认 `[hidden]` 行为，导致 `hidden` 标签仍显示。
- 弹窗内容过于像流程图/按钮组合，不符合当前操作场景。

修复：

- 增加全局 `[hidden] { display: none !important; }`。
- 简化完成说明确认弹窗，确认时隐藏驳回原因，驳回时显示原因。
- 去掉容易误导的完成说明流程块。

验证：

- 临时数据库 Playwright 验证：
  - 确认弹窗中驳回原因隐藏。
  - 驳回弹窗中驳回原因显示。

剩余风险：

- 后续所有弹窗新增隐藏字段时，应检查 `[hidden]` 是否生效。

## 按钮巡检范围

2026-06-06 已加 ID 校验的按钮类型：

- 项目卡片：打开项目执行。
- 工作台头部：打开资料夹、资料库详情。
- 我的任务/待办：打开项目。
- 任务：保存、删除、提交完成说明、确认完成说明、驳回完成说明。
- Due Date：打开改期窗口、批准改期、驳回改期。
- 交付物：确认、驳回。
- 风险：保存、删除。

后续计划：

- 增加 Playwright 按钮巡检用例，自动点击关键按钮并检查是否出现 `Not found`、空 ID、控制台错误。
- 每次新增 `data-action` 按钮时，同步补 ID 校验和按钮巡检。
