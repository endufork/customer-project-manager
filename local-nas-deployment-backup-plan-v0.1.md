# 本地 NAS 部署、备份与远程访问方案 v0.1

本文档整理当前阶段关于客户项目资料管理系统的部署环境、NAS 选型、备份、邮件通知和远程访问方案。当前建议是：短期优先采用本地 NAS + 内网服务器试用，后续如团队流程稳定，再评估是否迁移到阿里云。

## 1. 当前结论

短期不必急着上云。本系统目前仍处于业务流程、权限、任务闭环和文件归档规则逐步完善阶段，使用本地 NAS 更适合 25 人左右的小团队内部试运行。

推荐短期架构：

```text
固定 Windows 小主机 / 办公室服务器：运行 Web 系统和数据库
NAS：存项目文件、资料归档、数据库备份
GitHub：只存代码和需求文档
移动硬盘或第二存储位置：做离线/异地备份
VPN：供外出人员安全访问公司内网系统
```

不建议把真实客户资料、报价、PO、图纸、模型放进 GitHub。

## 2. 本地 NAS 方案

推荐系统部署方式：

```text
Web 服务：固定小主机 / 办公室服务器
数据库：小主机本地磁盘
项目文件根目录：NAS 共享目录
数据库备份：每日导出到 NAS
离线备份：每周同步到移动硬盘或另一台备份设备
```

建议不要把 SQLite 数据库直接放在 NAS 共享盘上长期运行。NAS 适合放文件和备份，数据库更适合放在运行系统的小主机本地磁盘，再定时备份。

推荐 NAS 目录结构：

```text
/CustomerProject
  /Projects
  /SharedProductFiles
  /DatabaseBackups
  /SystemBackups
  /ImportDropzone
```

系统中的项目根目录可以从当前本地路径逐步迁移到：

```text
\\NAS\CustomerProject\Projects
```

## 3. NAS 选型建议

首选推荐：

```text
Synology 群晖 DS925+ / DS923+
4盘位
适合 25 人左右小团队
```

选择理由：

- DSM 系统成熟，权限、共享文件夹、快照和备份工具比较省心。
- 适合公司内部资料库，而不是个人折腾型设备。
- 后续即使系统上云，NAS 仍可作为本地文件副本和备份仓库。

第二选择：

```text
QNAP 威联通 TS-464 / TS-464-8G
```

QNAP 硬件配置通常更强，但系统维护、安全配置和权限管理更需要 IT 经验。

性价比选择：

```text
UGREEN 绿联 NASync DXP4800 Plus
TerraMaster 铁威马 F4-424 Pro
```

这类设备硬件性价比高，但作为公司长期资料基础设施，优先级低于群晖和 QNAP。

## 4. 推荐购买配置与预算

推荐配置：

```text
群晖 DS925+ 或 DS923+
4 × 8TB NAS 专用硬盘
UPS
```

建议硬盘类型：

```text
WD Red Plus / Red Pro
Seagate IronWolf / IronWolf Pro
Toshiba N300
```

预算估算：

```text
基础够用：1.2万 ～ 1.6万
比较稳妥：1.6万 ～ 2.2万
更长期一点：2.2万 ～ 3万+
```

推荐方案：

```text
DS925+ / DS923+
4 × 8TB NAS 硬盘
UPS
预算按 2万元以内准备
```

使用 SHR / RAID5 后，4 × 8TB 大约可用 24TB。对于 25 人左右团队、项目文档、报价、PO、PDF、Word、Excel 和部分 3D 模型，预计可以使用较长时间。

注意：

```text
RAID 不是备份
NAS 不是备份
NAS 是集中存储，仍然需要第二份离线或异地备份
```

## 5. 备份策略

建议采用三层备份：

```text
代码：GitHub
数据库：每日导出到 NAS
项目文件：NAS 主存储 + 每周离线备份
```

推荐规则：

- 数据库每天自动备份到 NAS 的 `/DatabaseBackups`。
- 项目文件集中存放在 NAS 的 `/Projects`。
- NAS 每周同步到移动硬盘、第二台 NAS 或异地存储。
- 每月至少做一次恢复演练，确认备份真的可恢复。
- UPS 必须配，避免断电导致 NAS 或数据库写入损坏。

长期如果上云，NAS 仍可保留为：

```text
本地项目文件库
云端文件同步副本
数据库备份落地点
离线恢复仓库
```

## 6. 邮件通知

邮件通知可以在内网系统中使用，不要求系统部署到云端。

推荐逻辑：

```text
内网系统
→ 连接阿里企业邮箱 SMTP
→ 给 PM / 工程师企业邮箱发通知
```

可用于：

- 验证码登录
- 新任务分配
- Due Date 即将到期
- Due Date 修改待 PM 确认
- 文件提交待 PM 确认
- 交付物驳回/返工
- 风险创建或升级
- 项目状态变化

建议单独建立系统邮箱：

```text
projectbot@jinxiangsz.com
或 pm-system@jinxiangsz.com
```

不要用个人邮箱作为系统通知邮箱。

常见 SMTP 配置方向：

```text
SMTP 服务：阿里企业邮箱 SMTP
端口：465 SSL 或 587 STARTTLS
账号：系统通知邮箱
密码：邮箱密码或客户端授权码
```

实际端口和授权方式应以阿里企业邮箱后台配置为准。

## 7. 外出访问方案

如果系统放在公司内网，人在公司外要访问，推荐使用 VPN，而不是把系统端口直接暴露到公网。

推荐访问链路：

```text
员工电脑 / 手机
→ 连接公司 VPN
→ 进入公司内网
→ 打开项目系统地址
```

示例地址：

```text
http://192.168.x.x:8765
或
http://project-manager.local
```

需要的设备或能力：

```text
固定运行系统的小主机 / 服务器
NAS
支持 VPN 的路由器或防火墙
公司宽带公网 IP，或 DDNS / 合规远程访问方案
员工电脑安装 VPN 客户端
```

VPN 类型优先级：

```text
SSL VPN / WireGuard
OpenVPN
IPSec
L2TP/IPSec
```

如果公司没有公网 IP，可选方式：

```text
向运营商申请公网 IP
使用企业路由器的云 VPN / 远程访问服务
使用 Tailscale / ZeroTier 类虚拟组网工具
```

Tailscale / ZeroTier 对小团队很方便，但依赖第三方服务，企业合规性需要确认。

不要直接这样做：

```text
http://公网IP:8765
```

系统包含客户项目资料、报价、PO、图纸和模型，不应裸露在公网。

## 8. 与阿里云方案的关系

本地 NAS 方案适合短期和中期试运行。阿里云方案适合正式外部访问、高可用、异地团队、统一运维和更标准的线上系统。

阿里云推荐架构：

```text
ECS：运行 Web 系统
RDS：运行数据库
OSS：存项目文件
NAS 或本地 NAS：作为同步副本和备份
```

预算粗略参考：

```text
节省试用：300 ～ 800 元/月
小团队标准：800 ～ 1800 元/月
更正式上线：2000 ～ 5000+ 元/月
```

当前建议：

```text
先做本地 NAS + 内网系统
流程稳定后，再评估是否迁移到阿里云
```

## 9. 当前推荐落地步骤

第一阶段：

```text
购买 NAS、硬盘和 UPS
配置 NAS 共享目录和权限
固定一台小主机运行系统
项目根目录切换到 NAS 共享路径
配置每日数据库备份到 NAS
```

第二阶段：

```text
配置企业邮箱 SMTP
实现登录验证码和任务通知
配置公司 VPN
让 PM / 工程师小范围试用
```

第三阶段：

```text
完善权限角色
完善备份恢复演练
评估是否需要阿里云
评估是否需要异地备份或第二台 NAS
```

## 10. 推荐最终配置

短中期推荐：

```text
Synology DS925+ / DS923+
4 × 8TB NAS 专用硬盘
UPS
固定 Windows 小主机运行系统
数据库本地运行，每日备份到 NAS
项目文件放 NAS
每周离线备份
外出访问走 VPN
邮件通知走阿里企业邮箱 SMTP
```

这套配置可以作为 3～5 年内部资料基础设施，即使后续系统上云，也不会浪费。
