# 工程工作台需求文档 v0.1

## 1. 定位

工程工作台是项目资料库之上的执行层，用来管理工程介入后的责任人、下一步动作、Due Date、任务、阻塞和闭环。

项目资料库回答：

```text
这个项目是什么？客户、工厂、产品、文件和历史资料在哪里？
```

工程工作台回答：

```text
这个项目现在谁负责？下一步做什么？什么时候必须完成？有没有超期或阻塞？
```

两者使用同一套项目主数据，不重复维护客户、工厂、产品、联系人和项目基础信息。

## 2. 与项目资料库的关系

当前主分支项目资料库的核心逻辑保留：

- 一条 `projects` 记录对应一台设备、一个夹具、一次改造或其他具体工程对象。
- `intake_no` 在前端显示为 INQ号，用于前期工程支持。
- `equipment_no` 在前端显示为 WO号/内部设备号，用于正式工程执行。
- `project_groups` 在前端显示为客户产品/生产线，用于关联同一产品或产线下的多个项目和共用资料。

工程工作台直接读取 `projects`，不另建一套孤立项目。

推荐关系：

```text
客户集团 / 法人主体 / 工厂
  └─ 客户产品/生产线
      ├─ 共享资料
      ├─ INQ-001 测试机，未开WO，处于前期工程支持
      ├─ INQ-002 打标机，WO-088，处于WO工程执行
      └─ INQ-003 夹具，WO-089，处于WO工程执行
```

## 3. 工作区划分

工程工作台建议分为两个主工作区。

### 3.1 前期工程支持

适用于已有 INQ号、但未必已有 WO号的项目。

典型工作包括：

- 需求澄清
- 可行性判断
- 方案设计
- 风险评估
- 内部成本评估
- 内部报价
- 对外报价前的工程支持

前期支持也必须有责任人和 Due Date，因为这部分工程工作同样会影响报价和客户响应。

### 3.2 WO工程执行

适用于已经开出 WO号/内部设备号的项目。

典型工作包括：

- 工程设计
- BOM/图纸/模型输出
- 采购和外协
- 加工和装配
- 调试和验收
- 发货和交付
- 资料归档和项目关闭

进入 WO工程执行前，项目必须有 WO号/内部设备号。

## 4. 页面结构

### 4.1 工程工作台首页

首页重点显示“需要动作的项目”，而不是展示所有档案字段。

默认字段建议：

| 字段 | 说明 |
|---|---|
| INQ号 | 前期编号 |
| WO号 | 正式执行编号，未开 WO 时为空 |
| 客户 | 法人主体 |
| 工厂 | 客户工厂/站点 |
| 产品 | 客户产品/生产线 |
| 工程对象 | 设备/夹具/改造对象名称 |
| 工作区 | 前期支持 / WO执行 |
| 当前阶段 | 当前流程阶段 |
| 当前责任人 | 当前推动责任人 |
| 下一步动作 | 下一件必须完成的事情 |
| Due Date | 当前动作截止日期 |
| 状态 | 正常 / 即将到期 / 超期 / 阻塞 |
| 最近更新 | 最近一次执行记录时间 |

首页筛选建议：

- 全部
- 我的项目
- 前期支持
- 待开WO
- WO执行中
- 今日到期
- 7天内到期
- 已超期
- 阻塞
- 已关闭

### 4.2 工程项目详情

工程详情页建议分成四块。

第一块：项目基础信息，只读为主，来自项目资料库。

- INQ号
- WO号/内部设备号
- 客户集团、法人主体、工厂
- 客户产品/生产线
- 联系人
- 项目性质
- 工程对象名称
- 项目资料文件夹

第二块：当前推进状态。

- 工作区
- 当前阶段
- 当前责任人
- 下一步动作
- Due Date
- 阻塞状态
- 风险等级
- 最近更新

第三块：任务列表。

- 任务标题
- 所属阶段
- 责任人
- 状态
- 截止日期
- 完成日期
- 关联文件
- 备注

第四块：执行日志和文件。

- 状态变更记录
- 责任人变更记录
- 任务完成记录
- 文件上传记录
- 阻塞/解除阻塞记录
- 关联项目资料库文件

## 5. 生命周期建议

### 5.1 前期工程支持流程

```text
询价录入
→ 需求澄清
→ 方案设计
→ 内部评估/成本核算
→ 内部工程师报价
→ 已向客户报价
→ 等待客户反馈
→ 转WO / 丢单关闭 / 暂停
```

### 5.2 WO工程执行流程

```text
客户已下PO / 已开WO
→ 工程设计
→ 备料/采购
→ 装配/制作
→ 验收/调试
→ 已发货
→ 项目完成
```

第一版可以沿用当前 `project_statuses`，后续再拆成更细的执行阶段表。

## 6. 闭环规则

建议第一版强制以下规则：

- 只要项目未关闭，就必须有当前责任人。
- 只要项目未关闭，就必须有下一步动作。
- 只要有下一步动作，就必须有 Due Date。
- 状态变更时必须写入对应状态日期。
- 项目从前期支持进入 WO工程执行时，必须填写 WO号/内部设备号。
- 项目关闭时必须选择关闭原因：完成、丢单、暂停、取消、合并到其他项目、其他。
- 如果存在未完成任务，关闭时必须确认或填写说明。
- 如果项目超期，系统必须在列表中明显标记。

## 7. 任务模型建议

新增任务表，用于支撑工程工作台。

```text
execution_tasks
```

建议字段：

| 字段 | 说明 |
|---|---|
| id | 系统主键 |
| project_id | 关联项目资料库 `projects.id` |
| phase_code | 所属阶段 |
| title | 任务标题 |
| owner_name | 责任人 |
| status | open / doing / blocked / done / cancelled |
| due_date | 截止日期 |
| completed_at | 完成时间 |
| blocked_reason | 阻塞原因 |
| notes | 备注 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

项目表建议补充执行字段：

| 字段 | 说明 |
|---|---|
| execution_area | presales / wo_execution / closed |
| current_owner | 当前责任人 |
| next_action | 下一步动作 |
| current_due_date | 当前动作截止日期 |
| blocked_status | none / customer / technical / purchase / resource / other |
| risk_level | normal / watch / high |
| closed_reason | 关闭原因 |
| last_activity_at | 最近执行更新时间 |

## 8. 文件上传与归档

工程师在工作台上传文件时，不需要手动理解文件夹结构。

上传时只需要选择：

- 所属项目
- 所属任务，可选
- 文件类别
- 版本说明，可选

系统自动完成：

- 保存文件到项目资料库对应标准文件夹
- 写入 `project_files`
- 更新项目文件标记，例如是否有图纸、模型、验收资料
- 在执行日志里记录上传动作

INQ阶段文件归入当前项目的前期资料类别；WO阶段文件归入对应项目标准文件夹。客户产品/生产线共用资料仍放入共享资料层。

## 9. 提醒和看板

第一版提醒可以先在列表中实现，不必马上做系统通知。

建议标记：

- 今日到期
- 3天内到期
- 已超期
- 超过7天未更新
- 阻塞中

后续可扩展为：

- 本地桌面通知
- 邮件提醒
- Outlook 任务
- 企业微信/飞书/钉钉消息

## 10. MVP 开发拆分

### 第一阶段

- 新增工程工作台入口
- 工作台读取现有项目库数据
- 给项目增加当前责任人、下一步动作、Due Date、阻塞状态
- 新增任务列表
- 首页支持按超期、阻塞、前期支持、WO执行筛选

### 第二阶段

- 支持任务文件上传并自动归档回项目资料库
- 支持任务模板，例如前期方案、新设备、夹具改造、售后服务
- 支持关闭项目时的未完成任务检查
- 支持执行日志

### 第三阶段

- 支持工程师账号和权限
- 支持提醒通知
- 支持数据导出和统计
- 预留 Outlook、ERP、NAS、网盘接口

## 11. 待确认问题

1. 工程师责任人先用自由文本，还是建立人员表。
2. Due Date 是否只管理“下一步动作”，还是每个阶段都必须有阶段截止日。
3. 前期工程支持是否需要任务模板，还是先手动添加任务。
4. WO工程执行的阶段是否需要按项目性质使用不同模板。
5. 关闭项目前必须检查哪些文件，例如图纸、BOM、验收记录、照片。
