# 项目看板需求 v0.1

## 1. 文档目的

本文记录“项目看板”的第一版需求，用于后续开发、评审和团队试用。

项目看板不是项目资料库，也不是工程执行工作台。它是面向团队会议和日常跟进的聚合视图，目的是把项目事实、执行状态、责任人、风险和下一步动作放在同一个清晰页面中。

## 2. 模块定位

系统三个核心业务入口的边界如下：

```text
项目资料库：PM/Admin 维护项目事实和资料归档
工程执行工作台：PM/工程师处理任务、交付物、风险和确认
项目看板：全员查看项目重点状态、责任人和异常项
```

项目看板默认给所有登录用户可见，但不同角色的可操作入口不同：

- Admin / PM 可以从看板进入项目库详情和项目执行。
- Engineer 可以从看板进入项目执行和自己的任务。
- Readonly 只查看看板和项目摘要，不修改数据。

## 3. 使用场景

### 3.1 团队会议过项目

会议时需要快速回答：

```text
哪些项目超期？
哪些项目阻塞？
哪些项目有高风险？
哪些项目等待 PM 确认？
每个重点项目下一步是什么？
当前责任人是谁？
Due Date 是什么时候？
```

看板应优先展示异常项目，再展示正常推进项目，避免会议从大量正常项目中手动筛选问题。

### 3.2 PM 日常跟进

PM 需要快速看到：

- 待确认交付文件。
- 待确认完成说明。
- 待审批 Due Date 改期。
- 待处理风险关闭申请。
- 超期任务。
- 高风险或阻塞项目。

### 3.3 工程师查看相关项目

工程师需要快速看到：

- 与自己有关的项目。
- 自己负责的当前任务。
- Due Date。
- 是否存在阻塞或返工。
- 是否有需要补交的文件。

### 3.4 管理层只读查看

Readonly 用户需要看到项目整体状态，但不进入编辑流程。

## 4. 信息架构

第一版看板采用“先异常、再推进、再细节”的结构。

页面结构：

```text
顶部指标
→ 快速筛选
→ 项目分组列表
→ 项目快照抽屉
```

### 4.1 顶部指标

顶部只显示会议最常用指标：

- 进行中项目数。
- 本周到期任务数。
- 超期任务数。
- 阻塞项目数。
- 待 PM 确认事项数。
- 高风险项目数。

指标点击后可以切换对应筛选。

### 4.2 快速筛选

第一版筛选项：

```text
全部
需要处理
本周到期
超期
阻塞/高风险
待确认
我的相关
INQ
WO
```

筛选不做复杂高级查询，第一版只解决会议和日常查看的高频场景。

### 4.3 项目分组

建议默认分组：

```text
需要处理
正常推进
前期支持
已关闭 / 低关注
```

分组规则：

- 有超期、阻塞、高风险、待确认事项的项目进入“需要处理”。
- 有 WO 且任务正常推进的项目进入“正常推进”。
- 只有 INQ 或处于前期支持状态的项目进入“前期支持”。
- 已完成、已发货、已关闭项目进入“已关闭 / 低关注”。

## 5. 项目行显示字段

看板不采用完整 Excel 表格，也不采用大卡片堆叠。第一版使用紧凑的项目行卡片，每个项目一行。

每行默认字段控制在 8 个以内：

```text
当前编号：已有 WO 显示 WO，否则显示 INQ
客户/工厂
产品/产线
设备/夹具/改造名称
当前阶段
当前责任人
下一步动作
Due Date
```

右侧显示状态标记：

```text
超期
阻塞
高风险
待确认
返工
待 WO
```

设计原则：

- 项目编号、客户、产品、下一步动作必须一眼可见。
- 状态标记必须比普通字段更醒目。
- 不把项目库所有字段搬到看板。
- 不在看板行内展示长备注、完整文件列表或执行日志。

## 6. 项目快照抽屉

点击项目行后，在右侧打开项目快照抽屉。

快照抽屉显示：

- 项目基础信息摘要。
- 当前任务。
- 未关闭风险。
- 待确认交付物。
- 待确认完成说明。
- Due Date 改期申请。
- 最近 5 条执行日志。
- 进入项目执行按钮。
- 进入项目库详情按钮，PM/Admin 可见。

抽屉原则：

- 看板列表保持轻。
- 需要细节时才打开抽屉。
- 抽屉只显示摘要，不替代完整项目执行页。

## 7. 状态计算规则

项目看板状态由项目资料库和工作台数据共同计算。

推荐优先级：

```text
1. 有未完成任务超期：超期
2. 有阻塞任务或高风险：阻塞/风险
3. 有待确认交付物、完成说明、改期申请、风险关闭申请：待确认
4. 有返工任务：返工
5. 有进行中任务：进行中
6. 项目已开 WO 但无任务：待启动
7. 只有 INQ：前期支持
8. 项目已关闭：已关闭
```

如同一项目同时满足多个条件，看板显示最高优先级状态，同时保留其他状态标记。

## 8. 数据来源

项目看板只读聚合以下数据：

### 8.1 项目资料库

- 项目 ID。
- INQ。
- WO/内部设备号。
- 客户集团。
- 法人主体。
- 工厂。
- 产品/产线。
- 联系人。
- 项目名称。
- 项目性质。
- 项目状态。
- 当前状态日期。
- 项目文件夹路径。

### 8.2 工程执行工作台

- 任务总数。
- 已关闭任务数。
- 当前任务。
- 任务负责人。
- 最近 Due Date。
- 超期任务数。
- 阻塞任务数。
- 返工任务数。
- 待确认交付物数。
- 待确认完成说明数。
- 待审批改期数。
- 未关闭风险数。
- 高风险数。
- 最近执行日志。

## 9. 权限规则

第一版权限规则：

```text
Admin：查看全部，看板进入项目库和项目执行
PM：查看全部，看板进入项目库和项目执行
Engineer：查看全部，看板进入项目执行和我的任务
Readonly：查看全部，只读
```

暂时所有角色都可查看全部项目，因为团队会议和项目透明度需要一致视图。

后续如果信息过多，再增加个人默认筛选，而不是先做强隔离。

## 10. 与项目库、工作台的关系

### 10.1 与项目资料库

项目库仍是项目事实维护入口。

看板不直接编辑：

- 客户信息。
- 工厂信息。
- 产品/产线。
- INQ/WO。
- 项目文件夹路径。
- 文件索引。

### 10.2 与工程执行工作台

工作台仍是执行动作入口。

看板不直接完成：

- 创建任务。
- 上传文件。
- PM 确认/驳回。
- Due Date 审批。
- 风险关闭。

第一版看板只允许跳转或打开快照，不在看板内嵌复杂操作。

## 11. 第一版实现范围

第一版必须实现：

- 看板主入口。
- 顶部指标。
- 快速筛选。
- 项目行卡片列表。
- 项目状态标记。
- 项目快照抽屉。
- 进入项目执行。
- PM/Admin 进入项目库详情。
- 后端提供聚合 API。
- 避免 N+1 查询。
- 后端权限校验。

第一版暂不实现：

- 甘特图。
- 拖拽看板。
- 高级统计大屏。
- 自定义看板布局。
- 会议纪要自动生成。
- 复杂多条件高级查询。
- 看板内直接审批。

## 12. 后端接口建议

建议新增接口：

```text
GET /api/workbench/board
```

返回内容：

- 顶部指标。
- 项目列表。
- 每个项目的计算状态。
- 每个项目的摘要字段。
- 每个项目的待办计数。

接口实现要求：

- 使用明确 Pydantic Response Schema。
- 按项目批量读取任务、风险、交付物、Due Date 申请。
- 不在项目循环内反复执行 SQL 查询。
- 对无工作台数据的项目也要返回，状态为待启动或前期支持。

## 13. 前端建议

建议主导航调整为：

```text
项目看板
我的任务
项目执行
项目库
用户管理
```

显示规则：

- 项目看板：所有登录用户可见。
- 我的任务：所有登录用户可见。
- 项目执行：所有登录用户可见。
- 项目库：PM/Admin 可见。
- 用户管理：Admin 可见。

第一版可以先把“我的任务”保留在工作台内部，后续再独立成主导航。

## 14. 验收标准

第一版完成后，应至少验证：

- 登录后能进入项目看板。
- Engineer、PM、Readonly 都能查看看板。
- 非 PM/Admin 看不到项目库编辑入口。
- 有超期任务的项目排在前面。
- 有阻塞任务或高风险的项目有醒目标记。
- 有待确认事项的项目有待确认标记。
- 点击项目行能打开快照抽屉。
- 快照抽屉能进入项目执行。
- PM/Admin 能从快照进入项目库详情。
- 后端看板接口没有明显 N+1 查询。
- `.\tools\check.cmd` 通过。
- Playwright 冒烟通过。

## 15. 后续扩展

后续可以逐步增加：

- 跨项目风险总览页。
- 会议模式。
- 导出会议项目清单。
- 按客户/产品/负责人分组。
- 个人默认筛选。
- 项目状态建议联动。
- 报表导出。
- 邮件提醒。

