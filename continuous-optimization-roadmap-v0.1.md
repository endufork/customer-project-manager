# 系统模块后续优化路线图 v0.1

## 1. 文件定位

本文件用于持续记录“客户项目资料管理系统”的后续优化方向、讨论结论和阶段计划。

它不是一次性需求文档，而是长期维护的路线图。每次围绕系统方向、模块边界、流程闭环、权限、上线部署等进行讨论后，如形成明确结论，应更新本文件。

## 2. 持续优化原则

### 2.1 先闭环，再扩展

新增功能前先确认它是否能形成闭环：

```text
谁使用？
在哪个页面进入？
会改变什么状态？
谁确认？
是否写日志？
是否产生通知？
是否归档到资料库？
```

不能只增加入口，而不处理状态、责任人、文件和记录。

### 2.2 资料库与工作台分层

项目资料库记录“项目事实”：

- 客户集团、法人主体、工厂、联系人
- 产品/产线
- INQ、WO/内部设备号
- 项目文件夹、共享资料、文件索引

项目执行工作台记录“执行过程”：

- 任务
- 负责人
- Due Date
- 交付文件
- 风险/问题
- 状态流转
- 执行日志

### 2.3 UI 以减负为目标

工程师视角优先回答：

```text
我现在要做什么？
什么时候完成？
卡在哪里？
要交什么文件？
```

PM 视角优先回答：

```text
哪些项目超期？
哪些文件待确认？
哪些风险需要处理？
哪些 Due Date 变更待审批？
```

### 2.4 讨论结论要落地

讨论形成结论后，需要记录：

- 决策内容
- 适用模块
- 暂不做的内容
- 下一步动作
- 未确认信息

避免同一个问题反复口头讨论，后续开发找不到依据。

## 3. 项目资料库优化方向

当前定位：项目事实库，回答“项目是什么、资料在哪里、文件是否齐全”。

后续优化方向：

- 项目列表继续减负，默认只显示核心字段。
- 项目详情强化文件索引、文件分类、文件来源。
- 修改客户主体、工厂、项目群、WO 后，目录迁移逻辑继续做稳。
- 产品/产线共享资料层继续完善，避免单台设备资料夹塞入共用文件。
- 增加更好的导出、备份、恢复能力。
- 项目文件扫描入口保持前台可见，并明确扫描结果与差异同步。

后续重点：

```text
目录迁移安全性
文件索引准确性
共享资料逻辑
备份与恢复
```

## 4. 工程执行工作台优化方向

当前定位：执行过程管理，回答“谁负责、做到哪、卡在哪里、文件是否确认”。

后续优化方向：

- 完善“我的待办”。
- 任务状态流转更清晰。
- 交付文件形成闭环。
- 风险按产品/产线级、设备/WO级、任务级分层管理。
- Due Date 改期审批。
- 项目执行页面持续减负。

任务状态建议继续围绕：

```text
未开始
进行中
待资料
阻塞
已提交
返工
已确认
```

交付文件路径：

```text
工程师上传
→ 自动归档资料库
→ 生成交付物记录
→ 任务进入待 PM 确认
→ PM 确认后关闭任务
→ PM 驳回后进入返工
```

## 5. 登录与权限优化方向

当前方向：团队试用版采用企业邮箱验证码登录。

已确认：

- 企业邮箱体系：阿里企业邮箱。
- 允许域名：`jinxiangsz.com`。
- 初始账号：`rongkai@jinxiangsz.com`。
- 角色：`Admin`、`PM`、`Engineer`、`Readonly`。
- 同一用户可以拥有多个角色。
- 新用户默认只读，Admin 分配角色。

后续优化方向：

- SMTP 邮箱验证码登录。
- 后端 API 强制权限校验。
- 用户管理页面。
- 关键操作写审计日志。
- 权限不只靠前端隐藏按钮。

第一版角色边界：

```text
Engineer：看项目、更新自己的任务、上传文件、创建风险、申请改期。
PM：分配任务、确认文件、审批改期、处理风险、修改项目资料。
Admin：用户管理、系统设置、删除项目或文件记录。
Readonly：查看项目和状态，不修改。
```

## 6. Due Date 与审批优化方向

Due Date 是执行闭环中的关键字段，不能无理由随意修改。

已确认规则：

```text
任何 Due Date 修改都必须填写理由。
Engineer 修改 Due Date 只是提交申请。
PM/Admin 确认后，新的 Due Date 才正式生效。
PM/Admin 直接修改 Due Date 也必须填写理由并留历史记录。
```

后续优化方向：

- 工程师任务卡片增加“申请改期”。
- PM 我的待办增加“待确认改期”。
- 改期申请保留历史记录。
- 已有 Pending 改期时，不允许重复提交新的 Pending 申请。
- PM 驳回必须填写理由。
- 改期结果通知工程师。

## 7. 通知优化方向

第一版优先系统内通知，不急着做邮件通知。

PM 通知重点：

- 待确认交付文件
- 待确认 Due Date 改期
- 高风险
- 超期任务

工程师通知重点：

- 我的任务
- 返工任务
- 改期申请结果
- 与我相关的风险状态变化

后续在 SMTP 稳定后，再增加邮件通知。

## 8. 文件与目录管家优化方向

当前方向：系统替用户完成文件归档和索引，而不是让工程师手动想文件放哪里。

后续优化方向：

- 上传文件自动归入正确分类目录。
- 防止重复文件、重名文件混乱。
- 文件移动或重命名后，数据库索引同步。
- 文件扫描结果更清晰。
- 删除文件记录和实际文件分权限处理。
- Admin 才能做危险删除动作。

## 9. 文档解析与搜索优化方向

当前方向：轻量级文件内容索引，不做复杂全文搜索平台。

后续优化方向：

- Word、Excel、PDF 文本提取失败时记录错误日志。
- 搜索结果显示来源项目、文件夹、分类。
- 3D 文件只识别是否存在，不做在线预览。
- 后续支持按文件类型、上传人、任务、日期筛选。

## 10. 上线与部署优化方向

当前系统仍处于本地开发和内网试用准备阶段。

后续需要确认：

- 系统运行机器：固定 Windows 主机、小服务器或 NAS 旁边机器。
- 团队是否都在同一局域网。
- 项目资料目录是否迁移到 NAS 或固定共享盘。
- 数据库备份位置和周期。
- 服务如何随 Windows 启动自动启动。
- SMTP 配置如何保存，且不进入 Git。

试用建议：

```text
1 个 PM/Admin
2 到 3 个工程师
1 到 2 个真实新项目
先跑完整 INQ/WO/任务/交付/确认/改期/风险流程
```

## 11. 代码结构优化方向

后续开发继续避免把功能堆在单个文件中。

方向：

- 后端业务逻辑优先放在 `customer_m/modules/`。
- `customer_m/fastapi_app.py` 只负责应用创建、路由注册、静态资源和启动初始化。
- `customer_m/api/` 只负责 HTTP 路由、请求校验、权限依赖和响应组织。
- 数据库迁移集中放在 `customer_m/database.py`。
- 前端暂时仍在 `static/app.js` / `static/styles.css`，但新增大功能时应提取清晰函数边界。
- 新增 Python 第三方依赖写入 `requirements.txt`。
- 新增 Node/测试依赖写入 `package.json` / `package-lock.json`。

## 12. 建议开发顺序

当前建议顺序：

1. 在 `codex/team-trial-auth` 做登录、用户、角色、权限基础。
2. 做 Due Date 改期申请和 PM 审批。
3. 把“我的待办”改成真正基于登录用户，而不是手动输入负责人。
4. 做系统内通知。
5. 做用户管理页。
6. 再考虑 SMTP 邮件通知、部署自启动、备份策略。

## 13. 当前待确认事项

- SMTP 系统发信邮箱。
- SMTP host、port、SSL/TLS 和授权码。
- 系统最终运行机器。
- 项目资料目录是否迁移到 NAS。
- 数据库备份位置和周期。
- 工程师是否允许创建任务。
- 工程师是否允许关闭自己创建的风险。
- 第一批试用人员名单。

## 14. 2026-06-05 方向更新

### 分支基线

当前持续开发不再从旧 `main` 直接开始。后续团队试用、权限、工作台、FastAPI、项目状态联动等功能，默认从：

```text
codex/team-trial-auth
```

新开分支。

原因：

- `codex/team-trial-auth` 已包含当前最完整的资料库、工作台、登录权限和 PM/工程师视图。
- 旧 `main` 更适合作为稳定资料库基线，不适合作为团队平台功能起点。
- 先改旧 `main` 再反向合入功能分支，容易遇到前端/后端拆分结构冲突。

### 系统名称

系统可见名称统一改为：

```text
项目管理系统
```

### 文件夹同步

项目基础信息修改后，影响标准路径的字段应同步项目资料夹：

- 客户集团
- 法人主体
- 工厂
- 客户产品/生产线
- 联系人
- 项目/设备/夹具名称
- WO号/内部设备号

目录迁移时必须同步数据库中的 `project_folder_path` 和 `project_files.file_path`。

### FastAPI

FastAPI 重构确认可行，但必须独立分支推进：

```text
codex/fastapi-refactor
```

原则：先替换 HTTP 路由层，不重写业务模块，不改变前端 API 路径。

### 项目库状态与项目执行联动

确认可以联动，但不应直接互相覆盖。

推荐做法：

- 工作台生成“执行摘要”和“建议项目状态”。
- PM 确认后，才把建议状态应用到项目库状态。
- 第一阶段先做只读摘要，第二阶段再做 PM 应用建议状态。

建议分支：

```text
codex/project-status-link
```

### 本地服务重启问题

当前本地服务不会随电脑重启自动恢复。电脑重启、休眠或终端关闭后，需要重新运行：

```powershell
run_server.cmd
```

团队试用阶段应考虑：

- Windows 开机自启脚本。
- NAS/固定主机部署。
- 数据库和项目资料目录备份。

## 15. 2026-06-05 FastAPI Schema 与诊断能力更新

### 决策内容

FastAPI 重构进入“接口收紧和排错能力增强”阶段。后续团队试用前，不能再依赖宽松请求体和静默异常处理。

已固化：

- 请求体使用明确 Pydantic Schema。
- 请求模型默认禁止多余字段。
- 工作台写接口按业务动作拆分模型。
- API 请求记录统一耗时日志。
- 文件系统关键操作失败时记录完整上下文和异常堆栈。

### 适用模块

- FastAPI API 网关。
- 项目资料库写接口。
- 认证和用户管理接口。
- 工程工作台任务、风险、Due Date、交付物接口。
- 文件扫描、导入、上传、目录迁移、回收站归档。

### 暂不做的内容

- 暂不引入集中式日志平台。
- 暂不做复杂审计表替代日志。
- 暂不把所有扫描错误写入数据库明细表。
- 暂不引入数据库连接池，继续使用 SQLite timeout、WAL、busy_timeout 策略。

### 下一步动作

- 优化文件扫描 N+1 查询。
- 增加单文件失败不中断整个扫描的 `failed_files` 统计和错误明细。
- 增加 422 Schema 校验相关测试。
- 规划团队试用阶段日志落盘位置和保留周期。
- 补充文件系统失败场景的回归测试。

### 未确认信息

- 团队试用时日志文件放在系统运行目录、NAS 目录还是单独日志目录。
- 日志保留周期。
- 是否需要在前端显示最近扫描错误明细。
- 是否需要 Admin 页面查看文件系统错误和接口异常。
