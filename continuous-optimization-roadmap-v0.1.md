# 系统模块后续优化路线图 v0.1

## 1. 文件定位

本文件用于持续记录“客户项目资料管理系统”的后续优化方向、讨论结论和阶段计划。

它不是一次性需求文档，而是长期维护的路线图。每次围绕系统方向、模块边界、流程闭环、权限、上线部署等进行讨论后，如形成明确结论，应更新本文件。

## 2. 持续优化原则

### 2.1 先闭环，再扩展

新增功能前先确认它是否能形成闭环：

```text
谁使用？
在哪个页面进入？
会改变什么状态？
谁确认？
是否写日志？
是否产生通知？
是否归档到资料库？
```

不能只增加入口，而不处理状态、责任人、文件和记录。

### 2.2 资料库与工作台分层

项目资料库记录“项目事实”：

- 客户集团、法人主体、工厂、联系人
- 产品/产线
- INQ、WO/内部设备号
- 项目文件夹、共享资料、文件索引

项目执行工作台记录“执行过程”：

- 任务
- 负责人
- Due Date
- 交付文件
- 风险/问题
- 状态流转
- 执行日志

### 2.3 UI 以减负为目标

工程师视角优先回答：

```text
我现在要做什么？
什么时候完成？
卡在哪里？
要交什么文件？
```

PM 视角优先回答：

```text
哪些项目超期？
哪些文件待确认？
哪些风险需要处理？
哪些 Due Date 变更待审批？
```

团队会议视角优先回答：

```text
哪些项目需要今天处理？
当前责任人是谁？
下一步动作是什么？
Due Date 是什么时候？
哪些事项阻塞了项目推进？
```

### 2.4 讨论结论要落地

讨论形成结论后，需要记录：

- 决策内容
- 适用模块
- 暂不做的内容
- 下一步动作
- 未确认信息

避免同一个问题反复口头讨论，后续开发找不到依据。

## 3. 项目资料库优化方向

当前定位：项目事实库，回答“项目是什么、资料在哪里、文件是否齐全”。

后续优化方向：

- 项目列表继续减负，默认只显示核心字段。
- 项目详情强化文件索引、文件分类、文件来源。
- 修改客户主体、工厂、项目群、WO 后，目录迁移逻辑继续做稳。
- 产品/产线共享资料层继续完善，避免单台设备资料夹塞入共用文件。
- 增加更好的导出、备份、恢复能力。
- 项目文件扫描入口保持前台可见，并明确扫描结果与差异同步。

后续重点：

```text
目录迁移安全性
文件索引准确性
共享资料逻辑
备份与恢复
```

## 4. 工程执行工作台优化方向

当前定位：执行过程管理，回答“谁负责、做到哪、卡在哪里、文件是否确认”。

工作台 bug 记录文件：

```text
workbench-bug-log-v0.1.md
```

后续每次出现工作台按钮、状态流转、Due Date、交付物、风险、权限相关 bug，应追加记录到该文件。

后续优化方向：

- 完善“我的待办”。
- 任务状态流转更清晰。
- 交付文件形成闭环。
- 风险按产品/产线级、设备/WO级、任务级分层管理。
- Due Date 改期审批。
- 项目执行页面持续减负。

任务状态建议继续围绕：

```text
未开始
进行中
待资料
阻塞
已提交
返工
已确认
```

交付文件路径：

```text
工程师上传
→ 自动归档资料库
→ 生成交付物记录
→ 任务进入待 PM 确认
→ PM 确认后关闭任务
→ PM 驳回后进入返工
```

## 5. 登录与权限优化方向

当前方向：团队试用版采用企业邮箱验证码登录。

已确认：

- 企业邮箱体系：阿里企业邮箱。
- 允许域名：`jinxiangsz.com`。
- 初始账号：`rongkai@jinxiangsz.com`。
- 角色：`Admin`、`PM`、`Engineer`、`Readonly`。
- 同一用户可以拥有多个角色。
- 新用户默认只读，Admin 分配角色。
- 初始管理员邮箱自动拥有 `Admin + PM`，避免系统冷启动后无人可分配角色。
- `Admin` 和 `PM` 权限保持独立，不能在前端或后端把 `Admin` 隐式当作 `PM`。

后续优化方向：

- SMTP 邮箱验证码登录。
- 后端 API 强制权限校验。
- 用户管理页面。
- 关键操作写审计日志。
- 权限不只靠前端隐藏按钮。

第一版角色边界：

```text
Engineer：看项目、更新自己的任务、上传文件、创建风险、申请改期。
PM：分配任务、确认文件、审批改期、处理风险、修改项目资料。
Admin：用户管理、系统设置、删除项目或文件记录。
Readonly：查看项目和状态，不修改。
```

## 6. Due Date 与审批优化方向

Due Date 是执行闭环中的关键字段，不能无理由随意修改。

已确认规则：

```text
任何 Due Date 修改都必须填写理由。
Engineer 修改 Due Date 只是提交申请。
PM 确认后，新的 Due Date 才正式生效。
PM 直接修改 Due Date 也必须填写理由并留历史记录。
```

后续优化方向：

- 工程师任务卡片增加“申请改期”。
- PM 我的待办增加“待确认改期”。
- 改期申请保留历史记录。
- 已有 Pending 改期时，不允许重复提交新的 Pending 申请。
- PM 驳回必须填写理由。
- 改期结果通知工程师。

## 7. 通知优化方向

第一版优先系统内通知，不急着做邮件通知。

PM 通知重点：

- 待确认交付文件
- 待确认 Due Date 改期
- 高风险
- 超期任务

工程师通知重点：

- 我的任务
- 返工任务
- 改期申请结果
- 与我相关的风险状态变化

后续在 SMTP 稳定后，再增加邮件通知。

## 8. 文件与目录管家优化方向

当前方向：系统替用户完成文件归档和索引，而不是让工程师手动想文件放哪里。

后续优化方向：

- 上传文件自动归入正确分类目录。
- 防止重复文件、重名文件混乱。
- 文件移动或重命名后，数据库索引同步。
- 文件扫描结果更清晰。
- 删除文件记录和实际文件分权限处理。
- Admin 才能做危险删除动作。

## 9. 文档解析与搜索优化方向

当前方向：轻量级文件内容索引，不做复杂全文搜索平台。

后续优化方向：

- Word、Excel、PDF 文本提取失败时记录错误日志。
- 搜索结果显示来源项目、文件夹、分类。
- 3D 文件只识别是否存在，不做在线预览。
- 后续支持按文件类型、上传人、任务、日期筛选。

## 10. 上线与部署优化方向

当前系统仍处于本地开发和内网试用准备阶段。

后续需要确认：

- 系统运行机器：固定 Windows 主机、小服务器或 NAS 旁边机器。
- 团队是否都在同一局域网。
- 项目资料目录是否迁移到 NAS 或固定共享盘。
- 数据库备份位置和周期。
- 服务如何随 Windows 启动自动启动。
- SMTP 配置如何保存，且不进入 Git。

试用建议：

```text
1 个 PM
2 到 3 个工程师
1 到 2 个真实新项目
先跑完整 INQ/WO/任务/交付/确认/改期/风险流程
```

## 11. 代码结构优化方向

后续开发继续避免把功能堆在单个文件中。

方向：

- 后端业务逻辑优先放在 `customer_m/modules/`。
- `customer_m/fastapi_app.py` 只负责应用创建、路由注册、静态资源和启动初始化。
- `customer_m/api/` 只负责 HTTP 路由、请求校验、权限依赖和响应组织。
- 数据库迁移集中放在 `customer_m/database.py`。
- 前端 JS 已按功能拆入 `static/js/`，CSS 已按职责拆入 `static/css/`；新增功能应进入对应模块，不再继续堆进单个大文件。
- 新增 Python 第三方依赖写入 `requirements.txt`。
- 新增 Node/测试依赖写入 `package.json` / `package-lock.json`。

## 12. 建议开发顺序

当前建议顺序：

1. 在 `codex/team-trial-auth` 做登录、用户、角色、权限基础。
2. 做 Due Date 改期申请和 PM 审批。
3. 把“我的待办”改成真正基于登录用户，而不是手动输入负责人。
4. 做系统内通知。
5. 做用户管理页。
6. 再考虑 SMTP 邮件通知、部署自启动、备份策略。

## 13. 当前待确认事项

- SMTP 系统发信邮箱。
- SMTP host、port、SSL/TLS 和授权码。
- 系统最终运行机器。
- 项目资料目录是否迁移到 NAS。
- 数据库备份位置和周期。
- 工程师是否允许创建任务。
- 工程师是否允许关闭自己创建的风险。
- 第一批试用人员名单。

## 14. 2026-06-05 方向更新

### 分支基线

当前持续开发不再从旧 `main` 直接开始。后续团队试用、权限、工作台、FastAPI、项目状态联动等功能，默认从：

```text
codex/team-trial-auth
```

新开分支。

原因：

- `codex/team-trial-auth` 已包含当前最完整的资料库、工作台、登录权限和 PM/工程师视图。
- 旧 `main` 更适合作为稳定资料库基线，不适合作为团队平台功能起点。
- 先改旧 `main` 再反向合入功能分支，容易遇到前端/后端拆分结构冲突。

### 系统名称

系统可见名称统一改为：

```text
项目管理系统
```

### 文件夹同步

项目基础信息修改后，影响标准路径的字段应同步项目资料夹：

- 客户集团
- 法人主体
- 工厂
- 客户产品/生产线
- 联系人
- 项目/设备/夹具名称
- WO号/内部设备号

目录迁移时必须同步数据库中的 `project_folder_path` 和 `project_files.file_path`。

### FastAPI

FastAPI 重构确认可行，但必须独立分支推进：

```text
codex/fastapi-refactor
```

原则：先替换 HTTP 路由层，不重写业务模块，不改变前端 API 路径。

### 项目库状态与项目执行联动

确认可以联动，但不应直接互相覆盖。

推荐做法：

- 工作台生成“执行摘要”和“建议项目状态”。
- PM 确认后，才把建议状态应用到项目库状态。
- 第一阶段先做只读摘要，第二阶段再做 PM 应用建议状态。

建议分支：

```text
codex/project-status-link
```

### 本地服务重启问题

当前本地服务不会随电脑重启自动恢复。电脑重启、休眠或终端关闭后，需要重新运行：

```powershell
run_server.cmd
```

团队试用阶段应考虑：

- Windows 开机自启脚本。
- NAS/固定主机部署。
- 数据库和项目资料目录备份。

## 15. 2026-06-05 FastAPI Schema 与诊断能力更新

### 决策内容

FastAPI 重构进入“接口收紧和排错能力增强”阶段。后续团队试用前，不能再依赖宽松请求体和静默异常处理。

已固化：

- 请求体使用明确 Pydantic Schema。
- 请求模型默认禁止多余字段。
- 工作台写接口按业务动作拆分模型。
- API 请求记录统一耗时日志。
- 文件系统关键操作失败时记录完整上下文和异常堆栈。

### 适用模块

- FastAPI API 网关。
- 项目资料库写接口。
- 认证和用户管理接口。
- 工程工作台任务、风险、Due Date、交付物接口。
- 文件扫描、导入、上传、目录迁移、回收站归档。

### 暂不做的内容

- 暂不引入集中式日志平台。
- 暂不做复杂审计表替代日志。
- 暂不把所有扫描错误写入数据库明细表。
- 暂不引入数据库连接池，继续使用 SQLite timeout、WAL、busy_timeout 策略。

### 下一步动作

- 优化文件扫描 N+1 查询。
- 增加单文件失败不中断整个扫描的 `failed_files` 统计和错误明细。
- 增加 422 Schema 校验相关测试。
- 规划团队试用阶段日志落盘位置和保留周期。
- 补充文件系统失败场景的回归测试。

### 未确认信息

- 团队试用时日志文件放在系统运行目录、NAS 目录还是单独日志目录。
- 日志保留周期。
- 是否需要在前端显示最近扫描错误明细。
- 是否需要 Admin 页面查看文件系统错误和接口异常。

## 16. 2026-06-05 整体架构与需求计划

综合邮件提醒、BOM/采购系统联动和报表导出后的整体架构、功能需求和详细实施计划，已单独整理为：

```text
project-management-platform-architecture-plan-v0.1.md
```

## 19. 2026-06-12 项目看板需求方向

### 决策内容

新增“项目看板”作为项目资料库和工程执行工作台之间的聚合视图。

已确认：

- 项目看板面向所有登录用户可见。
- 项目资料库主要给 PM 维护项目事实和资料归档，Admin 负责系统设置、用户和危险操作。
- 工程执行工作台负责任务、交付物、风险、确认和执行日志。
- 项目看板负责会议式总览，不直接替代项目库或工作台。
- 看板优先显示异常、责任人、下一步动作和 Due Date。

### 适用模块

- 项目看板。
- 项目资料库摘要。
- 工程执行工作台摘要。
- 权限与导航。
- 后续报表导出。

### 第一版形式

第一版采用：

```text
顶部指标
→ 快速筛选
→ 项目总览
→ 跨项目风险总览
→ 右侧项目/风险快照抽屉
```

默认关注：

- 超期。
- 阻塞。
- 高风险。
- 待确认。
- 本周到期。
- 当前责任人。
- 下一步动作。

### 暂不做的内容

- 甘特图。
- 拖拽看板。
- 高级统计大屏。
- 看板内直接审批。
- 复杂自定义布局。

### 下一步动作

- 按 `project-board-requirements-v0.1.md` 实现后端聚合 API。
- 避免项目循环内 N+1 查询。
- 增加项目看板主入口。
- PM 可从看板进入项目库详情。
- 非 PM 不显示项目库编辑入口。
- 用 Playwright 验证看板打开、筛选、抽屉和跳转。

### 2026-06-12 实现更新

已完成：

- 项目看板 v1。
- 看板快照移除执行日志。
- 跨项目风险总览 v1。
- 风险总览支持筛选、风险快照和跳转项目执行。

下一步候选：

- PM 待处理中心。
- 我的任务绑定登录账号。
- 项目库状态联动建议。

## 20. 2026-06-12 PM 待处理中心

### 决策内容

新增独立 `PM待处理` 页面，作为 PM 的集中处理入口。

已确认：

- 项目看板负责全员总览和会议视图。
- 项目执行页负责单个项目内的任务、文件、风险处理。
- PM 待处理中心负责跨项目集中审批和确认，不替代看板，也不替代单项目执行页。
- 第一版只处理已经进入待处理状态的事项，不在该页面创建任务或修改项目事实。

### 适用模块

- 工程执行工作台。
- PM 待办。
- Due Date 改期审批。
- 文件交付确认。
- 风险关闭确认。
- 权限与导航。

### 第一版形式

第一版采用：

```text
分类指标
→ 类型筛选
→ 待处理队列
→ 右侧处理面板
```

聚合类型：

- 待确认交付文件。
- 待确认完成说明。
- 待审批 Due Date 改期。
- 待确认风险关闭。

### 暂不做的内容

- 暂不做通知红点。
- 暂不做批量审批。
- 暂不在 PM 待处理中心创建任务。
- 暂不在 PM 待处理中心直接修改项目库事实。

### 下一步动作

- 将我的任务绑定登录账号。
- 做系统内通知和未读计数。

## 21. 2026-06-12 登录与权限安全加固

本次基于代码审查和产品逻辑审查，确认登录权限进入团队试用前必须先处理安全与权限边界。

决策内容：

- 验证码错误次数必须在失败路径可靠落库，不能因为异常回滚导致防爆破失效。
- 验证码和会话过期判断统一使用 UTC aware datetime，避免 aware/naive 时间比较异常。
- SMTP 发信失败只影响验证码发送结果，必须记录日志并降级处理，不能让接口直接 HTTP 500。
- 初始管理员邮箱自动初始化 `Admin + PM`。
- `Admin`、`PM`、`Engineer`、`Readonly` 按后端返回的 `roles` 精确放行，`Admin` 不再隐式继承 PM 权限。
- 同时具备 `PM` 和 `Engineer` 的用户允许切换工作台视角。

适用模块：

- 登录验证码。
- 用户与角色管理。
- 项目库权限。
- 工程工作台和 PM 待处理中心。

暂不做：

- 不在本次实现邮件通知推送。
- 不在本次把“我的任务”完全迁移到用户 ID 绑定。
- 不引入复杂审批流或消息队列。

下一步动作：

- 继续推进系统内通知和未读计数。
- 后续将任务负责人从姓名匹配逐步迁移到登录账号绑定。
- 邮件提醒在 SMTP 参数和发送策略确认后再作为增强接入。

未确认信息：

- 第一批试用人员名单和角色。
- SMTP 生产配置和发信失败告警方式。
- 任务负责人账号绑定的迁移规则。

## 22. 2026-06-16 前端 CSS 模块拆分

本次将原集中式 `static/styles.css` 拆为 `static/css/` 下的多个职责模块，减少单文件继续膨胀的维护风险。

决策内容：

- 页面仍使用原生前端和当前 class 命名体系，不引入构建工具。
- `static/index.html` 直接按顺序加载 CSS 模块，保证级联顺序明确。
- `static/styles.css` 保留为兼容入口，只通过 `@import` 引入模块文件。
- CSS 模块边界先按现有页面和职责划分，后续修改相关页面时再继续精细化，不做大范围无收益重构。

适用模块：

- 登录和用户管理。
- 项目资料库和通用布局。
- 项目看板、风险总览、PM 待处理中心。
- 工程执行工作台、任务、交付物、风险、Due Date、日志。
- 详情抽屉、toast、确认弹窗。
- 响应式适配。

暂不做：

- 暂不引入前端打包器、CSS Modules、Sass 或组件框架。
- 暂不重命名大量既有 class。
- 暂不把 JS 迁移为 ES modules。

下一步动作：

- 新增样式必须进入对应 CSS 模块。
- 后续重构工作台或项目库时，可继续把过宽的通用样式拆为更明确的组件样式。
- 如果后续引入通知中心、报表或 BOM 模块，应新增独立 CSS 模块并同步更新 `ARCHITECTURE.md`。

未确认信息：

- 是否需要在团队试用前进一步拆分项目库和工作台 JS 事件绑定。
- 是否需要在后续阶段引入轻量前端构建流程。
- 做项目库状态联动建议。

## 23. 2026-06-16 项目库前端 JS 模块拆分

本次将 `static/js/project-library.js` 中的项目库列表、详情和格式化辅助拆出独立文件，保留原生脚本加载方式，不引入构建工具。

决策内容：

- 项目库 JS 继续使用当前全局函数调用模式，避免一次性迁移 ES modules 带来额外风险。
- `project-library.js` 只保留项目库入口壳层、页面切换和启动数据加载。
- `project-library-format.js` 负责项目编号、标记、执行摘要、状态日期和选项渲染等纯展示辅助。
- `project-library-list.js` 负责项目库列配置、列选择、筛选、排序、KPI 和表格渲染。
- `project-library-detail.js` 负责项目详情抽屉、编辑表单、扫描、删除和跳转项目执行。

适用模块：

- 项目资料库列表。
- 项目详情抽屉。
- 项目基础信息编辑。
- 文件扫描入口。
- 项目执行跳转入口。

暂不做：

- 暂不改写项目库业务逻辑。
- 暂不重命名既有 DOM id、class 或后端 API。
- 暂不把前端迁移到打包器或 ES modules。

下一步动作：

- 后续修改项目库时，新增列表逻辑进入 `project-library-list.js`，新增详情操作进入 `project-library-detail.js`。
- 如果后续项目库详情继续膨胀，可再按“编辑表单 / 文件扫描 / 删除归档”继续细拆。
- 工作台任务模块仍是下一候选拆分对象，但等实际改任务流程时再处理。

未确认信息：

- 项目库详情是否需要拆成独立的只读详情和编辑表单两个区域。
- 团队试用前是否需要进一步收紧项目库列表默认列。

### 相关文档

```text
engineering-workbench-gap-plan-v0.1.md
project-management-platform-architecture-plan-v0.1.md
```

### 相关文档

```text
project-board-requirements-v0.1.md
project-management-platform-architecture-plan-v0.1.md
```

## 18. 2026-06-05 工作台闭环第一版

### 决策内容

工作台闭环从“只有入口和状态”推进到“任务提交、PM确认、返工、风险关联、项目库摘要”的第一版。

已落地：

- 无文件任务必须提交完成说明。
- 无文件任务由 PM 确认后关闭，驳回后返工。
- 文件型任务仍通过上传交付物提交。
- 交付物被驳回后，不能直接确认旧文件，必须重新提交。
- 普通任务编辑不能绕过提交/确认闭环。
- 阻塞任务必须填写阻塞原因。
- 阻塞任务可以选择已有打开风险进行关联；未选择时自动生成任务级高风险。
- PM 待办增加待确认完成说明。
- 完成说明确认/驳回使用统一弹窗，不再使用浏览器 `prompt`。
- PM 自己负责的无文件任务支持提交并直接确认。
- 项目资料库列表增加执行摘要列。

### 适用模块

- 工程执行工作台。
- 项目资料库列表。
- 任务。
- 交付物。
- 风险。
- PM 待办。

### 暂不做的内容

- 暂不做复杂审批流。
- 暂不做 PM 文件型任务上传后一键确认，仍走交付物确认流程。
- 暂不做复杂风险选择器，当前先使用任务编辑区下拉选择已有风险。
- 当时暂不做跨项目风险总览；2026-06-12 已补第一版。
- 暂不把“我的任务”完全绑定登录账号。

### 下一步动作

- 优化阻塞任务关联风险的前端引导和风险选择体验。
- 评估 PM 文件型任务是否需要上传后一键确认。
- 调整项目库执行摘要列的前端显示密度。
- 继续完善“我的任务”视图和登录用户绑定。

### 相关文档

```text
engineering-workbench-gap-plan-v0.1.md
project-management-platform-architecture-plan-v0.1.md
```

该文档作为后续复盘、review、分支开发和团队试用前评估的架构基线。

当前确认：

- 系统按“轻量项目管理平台”推进，而不是单纯文件归档工具。
- 项目资料库管事实。
- 工程工作台管过程。
- 文件系统管资产。
- 通知模块管提醒。
- 报表模块管输出。
- 集成模块管外部系统。

当前优先级：

```text
1. FastAPI 稳定化
2. 工作台闭环
3. 登录权限 + 我的任务
4. 项目看板 v1
5. 报表导出 v1
6. 通知中心 v1
7. BOM 管理 v1
8. 邮件提醒
9. 采购系统联动
```

当前不优先做：

```text
复杂实时扫描
复杂 BOM 自动解析
采购系统深度写入
高级统计大屏
完整移动端
复杂审批流引擎
```

## 17. 2026-06-05 FastAPI 稳定化收尾

### 决策内容

FastAPI 底层稳定化阶段完成。当前不继续强化扫描性能，扫描保持轻量，优先准确、安全、可排错。

已落地：

- 日志落盘。
- 数据库备份 API。
- 明确启动方式。
- 严格 Pydantic Schema 测试。
- 关键接口测试补强。
- 扫描单文件失败不中断。

### 适用模块

- FastAPI API 网关。
- 日志与运维。
- 数据库备份。
- 项目文件夹扫描。
- 测试体系。

### 暂不做的内容

- 暂不做实时文件监听。
- 暂不做复杂扫描性能优化。
- 暂不做 Admin 页面查看日志和备份。
- 暂不做自动定时备份。

### 下一步动作

- 进入工作台闭环阶段。
- 团队试用前确认日志目录、备份目录和备份周期。
- 后续再做 Windows 开机自启和定时备份。

### 相关文档

```text
fastapi-stabilization-runbook-v0.1.md
fastapi-refactor-plan-v0.1.md
fastapi-refactor-checklist-v0.1.md
project-management-platform-architecture-plan-v0.1.md
```
