# 项目协作与版本管理规则

本文件是本仓库的项目级工作规则。以后在本项目中使用 Codex 或手动开发时，默认遵守这些规则。

## 分支规则

- `main` 代表当前可用、相对稳定的版本。
- 小修小补可以直接在 `main` 上提交，例如文案、轻量 bug、文档补充。
- 新功能、较大结构调整、实验方向使用 `codex/*` 分支。
- 分支命名示例：

```text
codex/engineering-workbench
codex/excel-export
codex/backup-settings
```

## 提交规则

- 每次 commit 只做一类事情。
- 提交信息使用：

```text
feat: 新功能
fix: 修复问题
docs: 文档
refactor: 重构
style: 界面样式
data: 测试数据或模板
chore: 配置和杂项
```

- 好的提交示例：

```text
feat: add project column picker
fix: clean stale project folders
docs: add engineering workbench requirements
```

## 推送与 PR 规则

- `push` 只是上传到 GitHub，不等于业务确认完成。
- 大功能分支建议通过 PR 合并回 `main`。
- PR 合并前至少确认：
  - 页面能打开。
  - 相关 API 正常。
  - `tools/check.cmd` 通过。
  - 没有客户资料、数据库、日志、缓存进入 Git。

## 自动检查

提交前应运行：

```powershell
.\tools\check.cmd
```

本仓库支持 Git pre-commit hook。安装后，每次 `git commit` 前会自动运行检查：

```powershell
.\tools\install-hooks.cmd
```

## 禁止提交

不要提交：

```text
data/customer_projects.db
D:\01_CustomerProject
客户报价、PO、图纸、模型等真实项目资料
server.log
server.err
server.pid
__pycache__
*.pyc
```

## 当前业务约定

- 项目资料库的一条项目对应一台设备、夹具、改造或其他具体工程对象。
- INQ号用于前期工程支持。
- WO号/内部设备号用于正式工程执行。
- 项目库显示当前编号：已有 WO 时显示 WO，未开 WO 时显示 INQ。
- 多台设备共用资料放在客户产品/生产线共享资料层。

## 持续优化与讨论规则

- 后续模块优化方向、讨论结论和阶段计划持续记录在：

```text
continuous-optimization-roadmap-v0.1.md
```

- 每次围绕系统方向、模块边界、权限、工作流、上线部署等形成明确结论后，应更新该文件。
- 讨论结论需要尽量写清楚：
  - 决策内容
  - 适用模块
  - 暂不做的内容
  - 下一步动作
  - 未确认信息
- 项目资料库负责“项目事实”，项目执行工作台负责“执行过程”，不要把两类职责混在一起。
- UI 优化默认以减轻工程师和 PM 负担为目标，优先展示“下一步该做什么”和“哪里需要处理”。
- 大功能仍使用 `codex/*` 分支讨论和实现；主分支只承载稳定版本、文档补充和轻量维护。

## 后续代码开发固化规则

### 开发前

- 每次开始开发前先确认当前分支和工作区状态：

```powershell
git status --short --branch
```

- `main` 只承载当前可用、相对稳定的项目资料库版本。
- 工程工作台、权限、备份、导出、解析引擎等较大功能必须放在 `codex/*` 分支。
- 开发工作台时优先使用 `codex/engineering-workbench-mvp` 或后续同类工作台分支。

### 业务边界

- 项目资料库记录“项目事实”：客户、工厂、联系人、产品/产线、INQ、WO、文件夹、文件索引。
- 工程工作台记录“执行过程”：任务、负责人、Due Date、交付物、风险、执行日志。
- 工作台不另建孤立项目，必须关联项目资料库中的 `projects`。
- 一个资料库项目仍然对应一台设备、夹具、改造或其他具体工程对象。
- 产品/产线共用文件和产品级风险放在客户产品/生产线共享层，不塞进单台设备资料夹。

### 工作台开发原则

- 优先做闭环，不单纯堆入口。每个新增功能都要回答：

```text
谁使用？
在哪个页面进入？
会改变什么状态？
谁确认？
是否需要写日志？
是否产生通知？
是否需要回归档到资料库？
项目列表摘要是否要更新？
```

- UI 默认降低工程师负担：少字段、少解释、少跳转，优先让用户看到“我下一步该做什么”。
- PM 视角关注跨项目推进：逾期、阻塞、高风险、待确认交付物。
- 工程师视角关注我的任务：负责人、状态、Due Date、交付文件、备注。
- 风险必须分层管理：
  - 产品/产线级：影响同一产品/产线下多个设备。
  - 设备/WO级：影响当前单台设备、夹具或改造。
  - 任务级：必须关联具体任务。
- 任务状态、交付物、风险和执行日志应尽量联动，避免只靠人工事后补记录。

### 代码结构

- 后端业务逻辑优先放在 `customer_m/modules/`，避免继续把功能堆进 `customer_m/server.py`。
- `customer_m/server.py` 只负责 HTTP 路由、请求解析和响应组织。
- 数据库迁移集中放在 `customer_m/database.py`，新增字段或表必须考虑旧数据库迁移。
- 前端暂时仍在 `static/app.js` / `static/styles.css`，但新增大功能时应优先提取清晰函数边界，避免超长逻辑互相缠绕。
- 不写死本机绝对路径。工具路径通过 `tools/runtime.ps1`、环境变量或 PATH 解析。
- 新增 Python 第三方依赖必须写入 `requirements.txt`。
- 新增 Node/测试依赖必须写入 `package.json` / `package-lock.json`。

### 功能模块拆分原则

- 代码优先按业务功能模块拆分，而不是长期堆在单个前端或后端大文件中。
- 后端模块边界：
  - `customer_m/server.py` 只负责服务启动、HTTP 方法入口和日志。
  - HTTP 读写、静态资源、JSON 响应放在独立基础模块。
  - API 路由分发放在独立网关或路由模块。
  - 项目资料库、文件目录管家、扫描解析、工程工作台、任务、风险、交付物、Due Date、用户权限、通知、审计、备份分别按功能进入独立模块。
  - 业务逻辑优先放在 `customer_m/modules/` 或清晰命名的功能包中，避免回流到 `server.py`。
- 前端模块边界：
  - 新增大功能时不要继续无边界堆进 `static/app.js`。
  - 优先按项目资料库、工作台、任务、风险、交付物、Due Date、登录权限、通知、用户管理等功能拆分。
  - CSS 后续也应按基础样式、布局、表单、项目库、工作台、弹窗、登录权限等主题拆分。
- 拆分节奏应保守：
  - 新功能优先进入新模块。
  - 修改旧功能时顺手迁移相关代码。
  - 每次拆分只处理一类职责，并运行 `.\tools\check.cmd`。
  - 不为了“看起来整齐”做大范围无收益重构。

### 验证规则

- 提交前必须运行：

```powershell
.\tools\check.cmd
```

- 修改前端交互或布局时，应尽量用浏览器或 Playwright 验证：
  - 页面能打开。
  - 关键按钮可点击。
  - 弹窗/抽屉可打开和关闭。
  - 文字不重叠，按钮尺寸不突兀。
  - 工作台窗口和资料库窗口不会互相打断流程。
- 修改文件扫描、上传、目录迁移时，必须确认不会误删客户资料。

### 当前工作台下一阶段

- 当前 gap 和下一步计划记录在：

```text
engineering-workbench-gap-plan-v0.1.md
```

- 下一阶段重点不是继续堆页面入口，而是打通：

```text
任务状态变化
→ 文件上传归档
→ PM确认/驳回
→ 任务关闭或返工
→ 风险/阻塞同步
→ 项目列表摘要更新
```
