# 项目执行工作台需求文档 v0.2

## 1. 定位

项目执行工作台是项目资料库之上的“执行管理层”，面向项目经理和工程师共同使用。

它的目标不是替代项目资料库，也不是做一个复杂 ERP，而是做一个清晰、轻量、逻辑通顺的简易 Jira 式系统：

```text
项目资料库回答：这个项目是什么？资料在哪里？
项目执行工作台回答：这个项目现在谁负责？下一步是什么？什么时候完成？卡在哪里？交付物是否确认？
```

项目资料库负责主数据和文件归档：

- 客户集团、法人主体、工厂、联系人
- 客户产品/生产线
- INQ号、WO号/内部设备号
- 项目文件夹、共享资料、文件索引

项目执行工作台负责过程管理：

- 项目阶段
- 工作包和任务
- 责任人
- Due Date
- 文件提交和确认
- 风险/问题
- 状态流转和执行日志

## 2. 设计原则

### 2.1 清晰优先

首页只展示执行管理需要的信息，不把项目资料库里的所有字段搬过来。

默认关注：

- 编号
- 客户/工厂/产品
- 工程对象
- 当前阶段
- 当前责任人
- 下一步动作
- Due Date
- 风险/阻塞
- 文件交付状态

### 2.2 项目阶段不等于任务状态

项目阶段表示项目大概走到哪里。

任务状态表示具体工作是否完成。

例如：

```text
项目阶段：WO执行 / 工程设计
任务状态：机械设计进行中、电气设计等待资料、BOM已提交待确认
```

### 2.3 支持并行和交叉

非标项目不适合强制走一条固定线性流程。

系统应支持：

- 机械设计和电气设计并行
- BOM、采购、加工、装配部分重叠
- 物料到位后先装配，再接线和调试
- 简单夹具、改造项目跳过部分节点
- 复杂项目增加自定义任务

### 2.4 自动化辅助，人工可覆盖

系统可以自动生成任务、自动计算到期状态、自动根据文件上传改变任务状态。

但项目经理必须可以手动调整：

- 任务是否需要
- 负责人
- Due Date
- 阶段
- 风险等级
- 是否确认交付物

## 3. 用户角色

### 3.1 项目经理

项目经理是执行闭环责任人。

主要能力：

- 查看所有项目和任务
- 创建/调整任务
- 指派负责人
- 设置和修改 Due Date
- 确认或驳回工程师提交的文件
- 标记风险和阻塞
- 推进项目阶段
- 关闭项目

### 3.2 工程师

工程师是具体任务执行人。

主要能力：

- 查看分配给自己的任务
- 查看所在项目的完整任务板和上下游依赖
- 更新自己任务的状态和备注
- 上传交付文件
- 标记等待资料或阻塞
- 回复项目经理的驳回意见

建议第一版采用“透明但有限编辑”的方式：

```text
工程师可以看整个项目的任务板，但只能对自己负责的任务更新状态和备注、提交完成说明、上传交付物、申请改期和执行风险动作。
```

这样可以让工程师看到上下游关系，避免非标项目中信息被切碎。

### 3.3 后续协作角色

当前不设置独立 Readonly。后续出现真实管理层只读用户或采购、装配、电气等独立权限需求时再扩展角色。

当前权限由后端角色和任务对象归属共同强制；前端隐藏按钮只用于减少误操作，不构成权限边界。

## 4. 与项目资料库的关系

工作台不另建孤立项目，直接关联项目资料库中的 `projects`。

推荐关系：

```text
客户集团 / 法人主体 / 工厂
  └─ 客户产品/生产线
      ├─ 共享资料
      ├─ INQ-001 测试机，未开WO，处于前期工程支持
      ├─ INQ-002 打标机，WO-088，处于WO工程执行
      └─ INQ-003 夹具，WO-089，处于WO工程执行
```

核心规则：

- 一个资料库项目仍然对应一台设备、一个夹具、一次改造或其他具体工程对象。
- INQ阶段可以先进入工作台，用于前期方案、内部报价、客户报价支持。
- 开 WO 后，同一个项目继续进入 WO执行阶段，不重新建孤立执行项目。
- 当前 INQ 与 WO 一一对应。一份报价包含多个设备时，每台设备分别创建项目和 INQ，后续分别补充 WO，不引入跨项目 Inquiry 父实体。
- 客户产品/生产线层用于关联这些执行对象和共享资料。

## 5. 核心对象

### 5.1 项目执行对象

对应资料库中的一条 `projects` 记录。

它在工作台中补充执行字段：

- 工作区：INQ前期支持 / WO执行 / 已关闭
- 当前阶段
- 当前责任人
- 下一步动作
- 当前 Due Date
- 风险等级
- 阻塞状态
- 最近执行更新时间

### 5.2 工作包

工作包用于组织任务，类似 Jira 中的 Epic/Component，但保持轻量。

建议工作包：

- 项目管理
- 前期方案
- 报价支持
- 机械设计
- 电气设计
- BOM/采购
- 装配
- 接线
- 调试
- 验收
- 发货
- 关闭归档

工作包不一定全部启用。

### 5.3 任务

任务是工作台最小执行单位。

每个任务至少包含：

- 标题
- 所属项目
- 所属阶段
- 所属工作包
- 负责人
- 状态
- Due Date
- 是否需要交付文件
- 交付物确认状态
- 备注

### 5.4 交付物

交付物是任务完成的证据，通常是文件。

例子：

- 大致方案
- 内部报价输入
- 客户报价资料
- 机械图纸
- 3D模型
- 电气图纸
- BOM
- 接线图
- 调试记录
- 验收资料
- 发货资料

### 5.5 风险/问题

风险/问题独立于任务存在。

原因：

- 有些风险跨多个任务
- 有些风险项目结束后仍然值得保留
- 风险记录可以用于复盘

## 6. INQ阶段流程

INQ阶段用于未正式开 WO 前的工程支持。

典型流程：

```text
询价录入
→ 需求澄清
→ 大致方案
→ 内部评估/成本估算
→ 内部报价支持
→ 客户报价支持
→ 等客户反馈
→ 转WO / 暂停 / 丢单关闭
```

建议任务模板：

| 任务 | 默认工作包 | 典型责任人 | 交付物 |
|---|---|---|---|
| 澄清客户需求 | 前期方案 | PM/工程师 | 澄清记录 |
| 输出大致方案 | 前期方案 | 工程师 | 方案文件 |
| 评估技术风险 | 前期方案 | 工程师 | 风险说明 |
| 提供内部报价输入 | 报价支持 | 工程师 | 报价输入/BOM估算 |
| 确认客户报价资料 | 报价支持 | PM | 报价文件 |

INQ阶段允许没有 WO号。

但进入 WO执行阶段前必须填写 WO号/内部设备号。

## 7. WO阶段流程

WO阶段用于正式工程执行。

典型流程：

```text
WO已开
→ 细化方案
→ 机械设计 + 电气设计
→ BOM/采购
→ 加工/来料
→ 装配
→ 接线
→ 调试
→ 验收
→ 发货
→ 项目关闭
```

注意：这不是强制线性流程。机械、电气、BOM、采购和装配可能交叉进行。

建议工作台用任务和依赖表达真实情况，而不是只靠单一项目状态。

例子：

```text
机械设计：进行中，Due 2026-06-10
电气设计：等待客户I/O点表，Due 2026-06-12
BOM：部分提交待确认
采购：长周期物料已下单，普通物料待BOM确认
装配：机架物料到位后可提前开始
```

## 8. 任务状态

建议任务状态：

| 状态 | 含义 |
|---|---|
| 未开始 | 已创建但尚未动作 |
| 进行中 | 责任人正在处理 |
| 等待资料 | 缺客户、内部或供应商信息 |
| 阻塞 | 任务无法继续，需要项目经理介入 |
| 已提交 | 已上传交付物，等待确认 |
| 需返工 | 交付物被驳回，需要修改 |
| 已确认 | PM确认交付物或任务结果 |
| 已完成 | 不需要文件确认的任务已关闭 |
| 已取消 | 任务不再需要 |

文件上传驱动状态：

```text
工程师上传交付物
→ 任务自动变为“已提交”
→ PM确认
→ 任务变为“已确认”
→ 如被驳回
→ 任务变为“需返工”
```

建议第一版把“已确认”作为有文件交付任务的完成状态。

## 9. 项目阶段自动更新

项目阶段可以由任务状态辅助判断，但不应完全自动锁死。

建议规则：

- 如果 INQ 阶段的“大致方案”和“内部报价输入”已确认，系统提示可进入“客户报价支持”。
- 如果填写 WO号，系统提示可切换到 WO执行，并生成 WO任务模板。
- 如果 WO阶段关键任务全部完成，系统提示可进入“验收/发货/关闭”。
- 如果存在未完成关键任务，关闭项目时必须填写说明或确认跳过。

系统自动给建议，项目经理最终确认。

## 10. Due Date 规则

Due Date 需要自动和手动结合。

### 10.1 自动生成

模板可以提供默认周期，例如：

- INQ 大致方案：创建后 2 天
- 内部报价输入：创建后 3 天
- 机械设计：WO后 7 天
- 电气设计：WO后 7 天
- BOM：WO后 10 天
- 调试：预计装配完成后 3 天

这些只是默认值。

模板默认周期按工作日计算，PM 可以在创建后人工覆盖 Due Date。公司节假日规则后续进入系统配置；未配置节假日时至少跳过周六、周日。

### 10.2 手动调整

项目经理可以根据项目难度、客户要求、来料日期、人员负载手动改。

### 10.3 项目当前 Due Date

项目列表上的 Due Date 不需要单独维护。

建议自动取：

```text
当前未完成任务中最早的 Due Date
```

如果项目经理手动设置了“下一步动作”，也可以绑定到一个具体任务。

## 11. 项目经理视图

项目经理首页应像简易 Jira 看板/列表。

默认列：

| 字段 | 说明 |
|---|---|
| 当前编号 | WO优先，否则INQ |
| 客户 | 法人主体 |
| 工厂 | 客户工厂/站点 |
| 产品 | 客户产品/生产线 |
| 工程对象 | 设备/夹具/改造对象 |
| 工作区 | INQ前期支持 / WO执行 |
| 当前阶段 | 项目阶段 |
| 当前责任人 | 当前推动人 |
| 下一步动作 | 最需要推进的动作 |
| Due Date | 最早未完成任务截止日 |
| 任务进度 | 已完成/总任务 |
| 风险 | 正常 / 关注 / 高风险 |
| 阻塞 | 无 / 等客户 / 等内部 / 等供应商 / 技术阻塞 |
| 最近更新 | 最后执行日志 |

常用筛选：

- 我的项目
- 我负责的任务
- INQ前期支持
- 待开WO
- WO执行中
- 今日到期
- 7天内到期
- 已超期
- 阻塞
- 高风险
- 有待确认交付物
- 已关闭

PM常用动作：

- 新建任务
- 批量改负责人
- 批量改 Due Date
- 确认交付物
- 驳回交付物
- 记录风险
- 推进阶段
- 关闭项目

## 12. 工程师视图

工程师视图重点是“我的任务”。

默认列：

| 字段 | 说明 |
|---|---|
| 任务 | 要做什么 |
| 项目编号 | WO优先，否则INQ |
| 客户/产品 | 快速识别项目 |
| 工作包 | 机械/电气/BOM/调试等 |
| 状态 | 当前任务状态 |
| Due Date | 截止日期 |
| 交付物 | 是否需要上传 |
| PM确认 | 待确认/已确认/驳回 |
| 备注 | 最近说明 |

工程师可操作：

- 开始任务
- 上传文件
- 标记等待资料
- 标记阻塞
- 写备注
- 查看项目完整任务板

工程师默认不可操作：

- 删除项目
- 关闭项目
- 确认自己的交付物
- 修改别人任务的负责人或 Due Date

## 13. 风险/问题模块

风险/问题必须独立展示，不能只作为备注。

建议字段：

| 字段 | 说明 |
|---|---|
| 项目 | 关联项目 |
| 标题 | 风险/问题标题 |
| 类型 | 客户资料缺失 / 内部资源 / 设计风险 / 采购风险 / 交期风险 / 质量风险 / 其他 |
| 来源 | 客户 / 内部 / 供应商 / 其他 |
| 严重度 | 低 / 中 / 高 |
| 负责人 | 谁负责跟进 |
| Due Date | 解决或确认时间 |
| 状态 | 打开 / 跟进中 / 已解决 / 已接受风险 / 已关闭 |
| 关联任务 | 可选 |
| 关联文件 | 可选 |
| 解决记录 | 处理说明 |

项目关闭后风险记录仍然保留。

关闭项目时，如果仍有打开的高风险问题，必须确认关闭原因。

## 14. 文件提交、确认和归档

工作台文件上传应直接归档到项目资料库。

上传时工程师只需要选择：

- 所属任务
- 文件类别
- 版本说明，可选

系统自动完成：

- 保存到项目资料库对应标准文件夹
- 写入文件索引
- 关联到任务交付物
- 生成上传日志
- 将任务状态改为“已提交”

PM确认后：

- 交付物状态变为“已确认”
- 任务状态变为“已确认”
- 项目进度自动更新

PM驳回后：

- 交付物状态变为“驳回”
- 任务状态变为“需返工”
- 必须填写驳回原因

共享资料规则：

- 和单台设备无关、属于客户产品/生产线共用的文件，归档到共享资料层。
- 和具体设备/夹具/改造相关的文件，归档到项目文件夹。

## 15. 模板

模板用于快速生成任务，但不能把项目锁死。

建议第一版模板：

| 模板 | 适用场景 |
|---|---|
| INQ前期支持 | 大致方案、报价支持 |
| 简单夹具 | 夹具/治具，任务少 |
| 老设备改造 | 关联旧 WO，有风险记录 |
| 标准设备 | 常规设备 |
| 非标设备 | 机械、电气、BOM、装配、调试较完整 |
| 产线项目 | 一个客户产品/产线下多个设备并行 |

模板生成后，项目经理可以：

- 删除不需要的任务
- 新增任务
- 调整负责人
- 调整 Due Date
- 调整是否需要交付文件

## 16. 数据模型建议

### 16.1 人员表

```text
people
```

字段建议：

- id
- name
- role
- department
- email
- active

第一版也可以先用自由文本责任人，但建议尽早建人员表，方便筛选“我的任务”。

### 16.2 执行任务表

```text
execution_tasks
```

字段建议：

- id
- project_id
- work_package
- phase_code
- title
- description
- owner_id
- owner_name
- status
- due_date
- started_at
- submitted_at
- confirmed_at
- completed_at
- is_required
- requires_deliverable
- blocked_reason
- notes
- created_at
- updated_at

### 16.3 交付物表

```text
task_deliverables
```

字段建议：

- id
- task_id
- project_id
- file_id
- deliverable_type
- version_note
- status
- submitted_by
- submitted_at
- confirmed_by
- confirmed_at
- reject_reason

### 16.4 风险/问题表

```text
execution_issues
```

字段建议：

- id
- project_id
- task_id
- title
- issue_type
- source
- severity
- owner_id
- owner_name
- status
- due_date
- resolution
- created_at
- updated_at
- closed_at

### 16.5 执行日志表

```text
execution_activity_logs
```

记录：

- 状态变更
- 负责人变更
- Due Date 变更
- 文件上传
- 文件确认/驳回
- 风险创建/关闭
- 项目阶段变更

## 17. 页面建议

### 17.1 项目执行首页

列表 + 筛选 + 轻量看板。

第一版优先做列表，因为列表更适合 PM 扫描多个项目。

后续可以加看板：

```text
INQ前期支持 | 待客户反馈 | WO执行 | 阻塞 | 待确认 | 已关闭
```

### 17.2 项目执行详情

建议结构：

- 顶部：项目基础信息和当前执行状态
- 左侧/主区：任务列表
- 右侧：风险/问题、交付物确认、执行日志

### 17.3 我的任务

面向工程师。

按 Due Date、状态、项目分组。

### 17.4 待确认交付物

面向项目经理。

集中处理工程师上传的文件确认。

### 17.5 风险/问题列表

跨项目查看所有打开风险。

## 18. MVP 范围

第一期只做最能闭环的内容：

- 工作台前端独立入口
- 读取项目资料库项目
- 项目执行列表
- 项目执行详情
- 任务创建、编辑、状态更新
- 负责人和 Due Date
- 文件上传后自动归档到资料库
- 文件提交后任务自动变为“已提交”
- PM确认/驳回交付物
- 风险/问题记录
- 执行日志

第一期暂不做：

- 复杂账号体系
- 甘特图
- 自动排产
- ERP深度集成
- 消息推送
- 移动端

## 19. 验收标准

第一期完成后，应能跑通以下场景。

### 场景 A：INQ 前期支持

1. 从项目资料库选择一个 INQ 项目进入工作台。
2. PM 创建“大致方案”任务，指派工程师并设置 Due Date。
3. 工程师上传方案文件。
4. 系统自动归档文件，并把任务改为“已提交”。
5. PM确认文件。
6. 任务变为“已确认”，项目列表进度更新。

### 场景 B：INQ 转 WO

1. 项目补充 WO号/内部设备号。
2. 系统提示进入 WO执行阶段。
3. PM选择 WO任务模板。
4. 系统生成机械、电气、BOM、采购、装配、调试等任务。
5. PM可删改任务和 Due Date。

### 场景 C：并行工程执行

1. 机械设计和电气设计同时进行。
2. 电气任务标记“等待资料”。
3. 机械任务提交图纸并等待确认。
4. BOM任务部分完成。
5. 项目列表能显示当前最早 Due Date、阻塞和待确认状态。

### 场景 D：风险记录

1. PM创建“客户未提供最终产品图纸”的风险。
2. 风险关联到电气设计或方案任务。
3. 风险显示在项目详情和跨项目风险列表中。
4. 项目关闭后风险记录仍保留。

## 20. 待继续细化

1. 是否第一版就需要登录账号，还是先用“负责人姓名 + 角色视图”模拟权限。
2. 工程师是否允许修改自己的 Due Date，还是只能申请延期。
3. 交付物确认是否只由 PM 确认，还是允许技术负责人确认。
4. 文件版本管理如何展示：只展示最新版本，还是保留所有版本列表。
5. 风险严重度是否需要影响项目整体状态。
6. 是否需要在项目资料库列表中显示“工作台状态摘要”。
