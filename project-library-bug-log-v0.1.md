# 项目资料库 Bug 记录

本文件记录项目资料库相关的关键缺陷、根因、修复和验证结果。后续项目库相关 bug 应持续追加到这里。

## PL-001 INQ 转 WO 或首次 WO 创建后项目目录缺失

- 日期：2026-06-11
- 严重级别：高
- 影响模块：项目资料库、项目文件夹管家、项目状态/编号流转
- 现象：
  - 首次创建项目时已经填写 WO 号，数据库里有项目记录，但物理项目文件夹没有生成。
  - 从 INQ 更新为 WO 后，项目文件夹内只保留了原本有文件的分类文件夹，空的标准分类文件夹消失。
  - 如果 INQ 项目原本没有放入任何文件，更新为 WO 后，整个项目文件夹消失。
  - 当前运行库只读审计发现 8 个项目记录的 `project_folder_path` 指向的磁盘目录不存在，其中包含 Ministud 相关 WO 项目。
- 根因：
  - `cleanup_empty_parent_dirs` 在目录迁移后会对旧父目录执行递归空目录清理。
  - INQ 改为 WO 本质上通常是同一父目录下的项目叶子目录改名，新 WO 目录仍位于旧父目录下面。
  - 递归清理旧父目录时会误删新 WO 目录中的空标准分类文件夹；如果新 WO 项目目录没有任何文件，整个新目录也会被当作空目录删除。
  - `move_project_folder_if_needed` 在成功移动已有目录后没有再次补齐标准分类文件夹，导致历史缺失目录不会自动恢复。
  - `rename_project_folder_to_wo` 在当前路径已是 WO 命名时直接返回，未检查目录是否实际存在，导致“已是 WO 但目录缺失”的项目无法通过重命名接口修复。
- 修复：
  - `cleanup_empty_parent_dirs` 改为只尝试删除传入的空父目录及其向上的空祖先目录，不再递归扫描和删除其子目录。
  - `move_project_folder_if_needed` 在目录移动或补建后统一调用 `ensure_standard_dirs`，确保项目目录始终包含标准分类文件夹。
  - `rename_project_folder_to_wo` 在路径已是 WO 命名时也调用 `ensure_standard_dirs`，使接口具备幂等修复能力。
- 回归测试：
  - 首次创建即填写 WO 号时，项目目录和标准分类文件夹必须存在。
  - 空 INQ 项目更新为 WO 后，新 WO 目录必须存在，旧 INQ 目录不再存在，标准分类文件夹完整。
  - INQ 项目只有部分分类文件夹且已有文件时，更新为 WO 后文件保留，缺失标准分类文件夹自动补齐。
  - 已是 WO 命名但物理目录缺失时，调用重命名接口应补建项目目录和标准分类文件夹。
- 验证结果：
  - `.\tools\check.cmd` 通过，包含新增项目目录流转测试。
  - 新增 `tools/audit_project_folders.py`，用于只读审计项目目录和标准分类子目录；加 `--repair` 时仅补建缺失目录，不删除、不移动、不改数据库。
  - 对当前运行库执行审计：32 个有效项目中发现 19 个目录问题，其中 8 个项目目录缺失，11 个项目标准分类子目录不完整。
  - 已执行 `python tools\audit_project_folders.py --repair` 补建缺失目录和标准分类子目录。
  - 修复后再次执行 `python tools\audit_project_folders.py`，结果为 0 个目录问题。
- 剩余风险：
  - 历史上被误删的空目录可以补建；如果曾有未被数据库索引且已被误删的真实文件，代码无法自动恢复，需要从备份、NAS 快照或回收站恢复。
  - 当前修复不会移动或删除任何现有客户资料。

## PL-002 历史产品/产线共享资料目录缺失

- 日期：2026-07-03
- 严重级别：中
- 影响模块：项目资料库、项目文件夹管家、历史目录重构工具、共享资料扫描
- 现象：
  - 只读审计 `project_groups.shared_folder_path` 发现 13 个客户产品/生产线分组中 6 个共享资料路径不存在。
  - 6 个缺失项均有关联项目，且产品/生产线父目录存在，缺失的是末级 `00_共享资料`。
  - 受影响分组包括 Deflector Drawer、Rear Drum、Body Grip、Early Entry Saw and Apache Trowel、Ministud、DEWALT PUSH And Self-propelled Mover。
- 根因：
  - 新建和编辑项目逻辑会创建 `00_共享资料`，但历史目录重构工具只补项目叶子目录的标准子目录。
  - 旧项目按新目录结构迁移时，工具没有校验 `project_groups.shared_folder_path`，导致历史分组中缺失的共享资料目录未被补建。
  - 本次 PO/报价文件迁移没有移动或删除共享资料目录；缺失点位于共享目录末级，且父产品/产线目录仍存在。
- 修复：
  - `tools/restructure_project_folders.py` 增加 active `project_groups` 共享资料目录校验。
  - 仅当共享路径位于项目根目录内且末级目录名为 `00_共享资料` 时才允许补建。
  - 已执行重构工具补建 6 个缺失共享资料目录，并同步补齐新增机械/电气标准子目录。
- 验证结果：
  - 补建前 dry-run：`Planned shared dirs to create: 6`，`Planned moves: 0`。
  - 执行 apply：`Created shared dirs: 6`，`Created dirs: 64`，`Moved: 0`。
  - 补建后 dry-run：`Planned shared dirs to create: 0`，`Planned dirs to create: 0`，`Planned moves: 0`。
  - 对 SC1288 执行项目文件扫描，结果为新增 2 个当前实际存在文件索引、移除 2 个缺失旧索引、失败 0 个。
  - SC1288 复查结果：`total_file_indexes=10`，`missing_file_indexes=0`。
- 剩余风险：
  - 本次修复只补建目录，不恢复目录中曾经可能存在但已丢失的文件。

## PL-003 Admin 全局扫描长期占用单个数据库事务

- 日期：2026-07-15
- 严重级别：中
- 影响模块：项目资料库扫描、共享资料扫描、SQLite 并发写入
- 现象：全局扫描把全部项目和共享目录放在同一个数据库连接和事务中；NAS 文件较多时，正常项目或任务写入可能等待并触发 busy timeout。
- 根因：扫描范围查询、文件枚举、哈希解析、索引更新和最终提交共用一个事务。
- 修复：先用只读连接取得扫描范围，再为每个项目和每个共享目录建立独立连接并单独提交；单个范围的数据库或文件错误记录为失败项，不回滚其他范围。随后增加 `file_scan_jobs` 后台任务，POST 立即返回任务，Admin 页面轮询持久化进度。
- 回归测试：全局扫描用例断言任务创建返回 `202`、后台完成后可查询统计、最近任务可恢复、一个项目和一个共享目录分别提交，以及重复创建时复用活动任务。
- 剩余风险：单个超大项目仍可能形成较长的项目级事务；当前后台任务运行在单个 FastAPI 进程内，服务重启会把任务标记为失败并要求重新发起。
