# 项目资料库 Bug 记录

本文件记录项目资料库相关的关键缺陷、根因、修复和验证结果。后续项目库相关 bug 应持续追加到这里。

## PL-001 INQ 转 WO 或首次 WO 创建后项目目录缺失

- 日期：2026-06-11
- 严重级别：高
- 影响模块：项目资料库、项目文件夹管家、项目状态/编号流转
- 现象：
  - 首次创建项目时已经填写 WO 号，数据库里有项目记录，但物理项目文件夹没有生成。
  - 从 INQ 更新为 WO 后，项目文件夹内只保留了原本有文件的分类文件夹，空的标准分类文件夹消失。
  - 如果 INQ 项目原本没有放入任何文件，更新为 WO 后，整个项目文件夹消失。
  - 当前运行库只读审计发现 8 个项目记录的 `project_folder_path` 指向的磁盘目录不存在，其中包含 Ministud 相关 WO 项目。
- 根因：
  - `cleanup_empty_parent_dirs` 在目录迁移后会对旧父目录执行递归空目录清理。
  - INQ 改为 WO 本质上通常是同一父目录下的项目叶子目录改名，新 WO 目录仍位于旧父目录下面。
  - 递归清理旧父目录时会误删新 WO 目录中的空标准分类文件夹；如果新 WO 项目目录没有任何文件，整个新目录也会被当作空目录删除。
  - `move_project_folder_if_needed` 在成功移动已有目录后没有再次补齐标准分类文件夹，导致历史缺失目录不会自动恢复。
  - `rename_project_folder_to_wo` 在当前路径已是 WO 命名时直接返回，未检查目录是否实际存在，导致“已是 WO 但目录缺失”的项目无法通过重命名接口修复。
- 修复：
  - `cleanup_empty_parent_dirs` 改为只尝试删除传入的空父目录及其向上的空祖先目录，不再递归扫描和删除其子目录。
  - `move_project_folder_if_needed` 在目录移动或补建后统一调用 `ensure_standard_dirs`，确保项目目录始终包含标准分类文件夹。
  - `rename_project_folder_to_wo` 在路径已是 WO 命名时也调用 `ensure_standard_dirs`，使接口具备幂等修复能力。
- 回归测试：
  - 首次创建即填写 WO 号时，项目目录和标准分类文件夹必须存在。
  - 空 INQ 项目更新为 WO 后，新 WO 目录必须存在，旧 INQ 目录不再存在，标准分类文件夹完整。
  - INQ 项目只有部分分类文件夹且已有文件时，更新为 WO 后文件保留，缺失标准分类文件夹自动补齐。
  - 已是 WO 命名但物理目录缺失时，调用重命名接口应补建项目目录和标准分类文件夹。
- 验证结果：
  - `.\tools\check.cmd` 通过，包含新增项目目录流转测试。
  - 新增 `tools/audit_project_folders.py`，用于只读审计项目目录和标准分类子目录；加 `--repair` 时仅补建缺失目录，不删除、不移动、不改数据库。
  - 对当前运行库执行审计：32 个有效项目中发现 19 个目录问题，其中 8 个项目目录缺失，11 个项目标准分类子目录不完整。
  - 已执行 `python tools\audit_project_folders.py --repair` 补建缺失目录和标准分类子目录。
  - 修复后再次执行 `python tools\audit_project_folders.py`，结果为 0 个目录问题。
- 剩余风险：
  - 历史上被误删的空目录可以补建；如果曾有未被数据库索引且已被误删的真实文件，代码无法自动恢复，需要从备份、NAS 快照或回收站恢复。
  - 当前修复不会移动或删除任何现有客户资料。
