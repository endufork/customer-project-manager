# 新项目录入页面规格 v0.2

## 1. 本版变化

本版本调整新项目录入逻辑：

- 创建项目时不再强制要求 WO号/内部设备号。
- 系统自动生成 INQ号。
- WO号/内部设备号可后续补充。
- 支持客户集团、法人主体、工厂/站点、部门/业务单元。
- 支持客户产品/生产线，用于管理同一客户产品背景下的新设备、改造、夹具、备件和共享资料。
- 支持项目性质，用于区分新设备、老设备改造、夹具/治具、备件/耗材、售后/服务、纯方案/报价等。
- 支持 PO/采购主体与需求来源工厂分开记录。
- 币种默认 `CNY`，可选择 `USD`。
- 项目根目录默认使用 `D:\01_CustomerProject`。

## 2. 推荐流程

```text
Step 1 基础信息
→ Step 2 创建项目文件夹
→ Step 3 放入资料并扫描
→ Step 4 待办和保存
→ 进入项目详情
```

## 3. Step 1 基础信息

### 3.1 系统自动生成

| 字段 | 规则 | 示例 |
|---|---|---|
| INQ号 | `INQ-YYYYMMDD-序号` | `INQ-20260527-001` |
| 项目来源 | 固定为新项目 | `new` |
| 初始状态 | 默认询价录入 | `询价录入` |

### 3.2 必填字段

| 字段 | 控件 | 默认值 | 校验 |
|---|---|---|---|
| 客户公司/法人主体 | 下拉选择 + 新建 | 无 | 必填 |
| 项目/设备/夹具名称 | 文本输入 | 无 | 必填 |
| 项目性质 | 下拉选择 | 新设备 | 必填 |
| 当前状态 | 下拉选择 | 询价录入 | 必填 |
| 币种 | 下拉选择 | CNY | 必填 |

### 3.3 可选字段

| 字段 | 控件 | 说明 |
|---|---|---|
| 客户集团 | 文本输入 + 历史选项 | 大型跨国客户建议填写 |
| 工厂/站点 | 文本输入 + 历史选项 | 用于区分不同工厂来源 |
| 客户产品/生产线 | 文本输入 + 历史选项 | 能对应客户产品、产品线或生产线时填写 |
| 部门/业务单元 | 文本输入 + 历史选项 | 例如 Engineering、Maintenance、NPI，可选已有值也可手输 |
| 项目来源角色 | 下拉选择 | 工程师、项目经理、采购、维护、NPI、其他 |
| PO/采购主体 | 文本输入 + 历史选项 | 默认同客户公司/法人主体 |
| 关联原项目/原WO号 | 文本输入 + 历史选项 | 老设备改造、夹具/治具、备件时填写旧项目或旧WO号 |
| WO号/内部设备号 | 文本输入 + 历史选项 | 如果暂时没有可留空，正式开 WO 后填写 |
| 客户联系人 | 下拉选择 + 新建 | 若暂不确定可留空 |
| 询价日期 | 日期选择 | 默认今天 |
| 预计报价日期 | 日期选择 | 可用于自动待办 |
| 预计交期 | 日期选择 | 后续执行提醒 |
| 备注 | 多行文本 | 记录初始背景 |

### 3.4 WO号/内部设备号校验

WO号为空时允许保存。

WO号填写后：

- 不允许重复。
- 前后空格自动去除。
- 大小写不敏感。
- 允许英文字母、数字、横杠、下划线。

推荐正则：

```text
^[A-Za-z0-9_-]+$
```

## 4. Step 2 创建项目文件夹

保存项目时，系统创建标准项目文件夹。此时不要求文件已经全部准备好。

推荐日常方式：

```text
创建项目
→ 打开项目文件夹
→ 把邮件附件、微信文件、桌面文件等散乱资料放入对应目录
→ 如果有共用资料，放入客户产品/生产线的 `00_共享资料`
→ 点击扫描项目文件
```

如果项目填写了客户产品/生产线，系统会额外创建共享资料目录：

```text
客户集团/
  客户公司/法人主体/
    工厂/站点/
      02_客户产品项目/
        客户产品/生产线/
          00_共享资料/
          设备项目文件夹/
```

### 4.1 可选资料包导入

如果已有完整资料包，也可以填写资料包路径。系统会复制资料包中的文件到项目目录。

### 4.2 文件处理规则

资料包导入时：

- 复制文件到系统项目目录。
- 保留原始文件名。
- 记录原始来源路径。
- 不移动、不删除原始文件。
- 如果目标目录已有同名文件，自动增加序号，例如 `文件名 (2).pdf`。

### 4.3 项目目录

项目根目录：

```text
D:\01_CustomerProject
```

目录编号规则：

- 如果填写了 WO号/内部设备号，目录使用 WO号/内部设备号。
- 如果未填写 WO号/内部设备号，目录使用 INQ号。

示例：

```text
D:\01_CustomerProject\
  Customer Group\
    ABC Company\
      Factory A\
        01_独立项目\
          INQ-20260527-001_ZhangSan_TestDevice\
            01_输入资料\
            02_报价与订单\
              01_内部报价\
              02_对客报价\
              03_PO订单\
            03_方案与图纸\
              01_机械\
              02_电气\
            04_BOM与采购\
              01_BOM\
              02_采购商务\
            05_装配调试\
            06_验收发货\
            99_其他\
        02_客户产品项目\
          Product Line A\
            00_共享资料\
            INQ-20260527-002_LiSi_MarkingMachine\
              01_输入资料\
              02_报价与订单\
                01_内部报价\
                02_对客报价\
                03_PO订单\
              03_方案与图纸\
                01_机械\
                02_电气\
              04_BOM与采购\
                01_BOM\
                02_采购商务\
              05_装配调试\
              06_验收发货\
              99_其他\
```

## 5. Step 3 放入资料并扫描

用户手动把资料放入项目文件夹后，在项目详情顶部点击“扫描项目文件”或“一键扫描”。系统不移动这些文件，只建立索引、提取文本并更新项目标记。

扫描入口：

- `扫描项目文件`：只扫描当前项目文件夹。
- `扫描共享资料`：只扫描当前客户产品/生产线的 `00_共享资料`。
- `一键扫描`：先扫描当前项目文件夹；如项目已关联客户产品/生产线，再扫描对应共享资料。未关联共享层时只返回跳过说明，不做全库批量扫描。

全局扫描属于系统维护能力，仅 Admin 可用；用于定期扫描全部有效项目文件夹和共享资料层，并对文件索引做对账。

| 字段 | 说明 |
|---|---|
| 文件名 | 当前文件名 |
| 扩展名 | 例如 `.pdf`、`.xlsx`、`.stp` |
| 自动分类 | 系统识别的分类 |
| 文件大小 | 便于确认 |
| 是否 3D 模型 | 自动识别 |
| 来源路径 | 原始文件路径 |

分类优先级：

1. 如果文件已经放在标准分类目录中，优先使用所在目录作为分类。
2. 3D 模型和图纸后缀进入 `图纸模型`。
3. 文件名包含 PO、订单、purchase order 进入 `PO 订单`。
4. 文件名包含报价、quote、quotation 进入 `客户报价`。
5. 文件名包含内部报价、成本、cost 进入 `内部报价`。
6. 文件名包含方案、proposal、solution、spec 进入 `方案资料`。
7. 文件名包含询价、RFQ、需求、requirement 进入 `询价需求`。
8. 无法识别进入 `其他`。

3D 模型后缀：

```text
.step .stp .sldprt .sldasm .dwg .dxf .iges .igs .x_t .x_b .prt .asm
```

## 6. Step 4 待办和保存

### 6.1 自动建议待办

| 场景 | 自动建议 |
|---|---|
| 未填写 WO号/内部设备号 | 创建“补充WO号”待办，可选 |
| 填了预计报价日期 | 创建报价截止待办 |
| 状态为已向客户报价 | 创建客户跟进待办 |
| 填了预计交期 | 创建交期提醒 |
| 文件分类中没有报价文件 | 可提示后续补报价 |

### 6.2 保存后动作

保存成功后：

- 创建客户和联系人，如为新建。
- 创建项目。
- 创建项目标准目录。
- 如填写资料包路径，则复制文件。
- 用户点击重新扫描后写入文件索引。
- 对 PDF/Word/Excel/TXT/CSV 提取文本。
- 写入项目事件时间线。
- 进入项目详情页。

## 7. 保存校验

### 7.1 必须阻止保存

- 客户公司为空。
- 项目/设备/夹具名称为空。
- 币种为空。
- WO号/内部设备号格式不合法。
- WO号/内部设备号重复。
- 项目文件根目录未配置。
- 目标项目目录创建失败。
- 文件复制失败且用户选择“失败即停止”。

### 7.2 可以允许保存但提示

- 未填写 WO号/内部设备号。
- 未选择联系人。
- 未扫描到文件。
- 未填写预计报价日期。
- 未填写预计交期。
- 文件分类存在“其他”。
- 文本提取失败。

## 8. WO号后补流程

项目详情页提供“补充WO号/内部设备号”操作。

```text
输入 WO号/内部设备号
→ 校验格式
→ 校验唯一性
→ 保存到项目
→ 记录项目事件
→ 提示是否重命名项目文件夹
```

系统不会静默自动重命名。用户确认后，只重命名项目叶子文件夹，并同步更新项目目录路径和文件索引路径；用户取消时保留原 INQ 文件夹名。

## 9. 当前仍需确认

1. INQ号格式是否接受 `INQ-YYYYMMDD-001`。
2. 备份目录最终选移动硬盘、NAS 还是网盘同步目录。
3. 是否需要在第一版支持欧元等其他币种。
