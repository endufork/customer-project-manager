# 项目库状态与项目执行联动计划 v0.1

## 当前判断

项目库状态可以和项目执行状态联动，但不能简单地互相覆盖。

原因：

- 项目库状态是项目事实，例如询价、报价、PO、生产、发货、完成。
- 项目执行状态是执行过程，例如任务未开始、进行中、已提交、返工、已确认。
- 一个项目可能有多个任务并行，不能因为某个任务完成就直接把项目状态推进到下一阶段。

## 推荐逻辑

### 项目库状态

由 PM 负责确认，代表项目对外/整体状态。

示例：

```text
询价
方案/澄清
报价中
已报价
已开WO
设计/采购/装配/调试
验收/发货
完成
```

### 工作台状态

由任务、交付物、风险、Due Date 组成，代表内部执行状态。

示例：

```text
未开始
进行中
已提交
需返工
等待资料
阻塞
已确认
```

## 联动方式

项目执行不直接覆盖项目库状态，而是提供“建议状态”和“状态摘要”。

例如：

- 所有 INQ 前期任务已确认，可建议项目状态进入“已报价/等待客户反馈”。
- 已填写 WO 号，可建议项目状态进入“已开WO”。
- 设计任务完成、BOM任务进行中，可摘要显示“设计完成，采购中”。
- 有阻塞任务或高风险，项目库显示风险标记，但不自动改项目状态。

## 页面表现

项目库列表中可增加：

```text
执行摘要
当前执行节点
当前负责人
最近Due Date
阻塞/风险标记
待PM确认数
```

项目详情中增加：

```text
执行状态摘要
建议项目状态
应用建议状态按钮
```

## PM 确认原则

以下变化需要 PM 确认后才写入项目库状态：

- 从 INQ 进入 WO。
- 报价完成。
- 生产/装配阶段切换。
- 验收/发货。
- 项目完成或关闭。

## 分支

建议从当前集成分支拉出：

```text
codex/team-trial-auth -> codex/project-status-link
```

先做只读摘要，再做 PM 应用建议状态。
