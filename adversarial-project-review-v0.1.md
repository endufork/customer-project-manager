# 项目需求与代码对抗式审查 v0.1

## 1. 文档定位

本文记录 2026-07-15 对整个项目需求、业务流程、代码安全、开发流程和文档完整性的对抗式审查结果。

本文不是新的功能需求基线，也不代表所有整改方向已经确认。它用于：

- 固化已经发现的风险和证据。
- 区分可以直接修复的问题与需要业务负责人确认的产品决策。
- 给出团队试用前的整改顺序和退出条件。
- 为后续每类整改建立独立提交和回归测试入口。

审查基线：

```text
分支：codex/file-visibility-permissions
提交：b9f9d78 fix: secure and lock project dependencies
日期：2026-07-15
```

## 2. 审查范围与方法

审查范围：

- 当前需求基线、产品说明、架构计划和持续优化记录。
- FastAPI 路由、认证授权、工作台业务模块、文件系统操作和 SQLite 数据模型。
- 项目看板、项目资料库、工作台、PM 待处理、用户管理和窄屏页面。
- 分支、提交、检查脚本、Python 测试、Playwright E2E 和依赖锁。
- NAS、SMTP、备份、恢复、日志、权限和团队试用文档。

验证结果：

```text
.\tools\check.cmd：通过
Python：46 passed
Playwright E2E：4 passed
npm audit：0 vulnerabilities
工作区：干净
```

浏览器补充审查使用真实本地服务完成登录、主要页面、详情抽屉、键盘 Escape 和 390px 窄屏验证。该检查只浏览现有业务数据，没有创建、修改或删除业务记录。

## 3. 第一性原理判断

### 3.1 系统真正需要解决的问题

本系统的第一性目标不是“把所有项目字段放进网页”，而是让团队持续回答并可靠执行以下问题：

```text
这是什么项目？
当前谁负责？
下一步是什么？
什么时候完成？
卡在哪里？
谁需要确认？
交付文件在哪里？
发生过什么变化？
系统故障或误操作后能否恢复？
```

当前项目在“项目事实与执行过程分离”“文件只复制、不破坏原资料”“任务、风险、交付物审批闭环”方面符合第一性原则。

当前主要偏差是：

- 系统希望成为团队执行入口，但仍依赖用户主动打开页面发现待办。
- 系统已经进入多用户阶段，但部分需求基线仍按单人 MVP 描述。
- 页面过滤已经存在，但真实共享目录权限尚未闭环。
- 流程已经绑定角色，但部分写接口没有绑定具体责任对象。
- 功能清单和验收标准多于当前实际实现，缺少可追踪的范围收口。

### 3.2 当前阶段判断

当前版本适合：

```text
单机受控演示
开发验证
少量可信用户的流程试跑
```

当前版本暂不适合：

```text
直接开放 25 人局域网使用
宣称客户报价和 PO 已完成物理权限隔离
依赖当前备份接口承担灾难恢复
以当前需求文档直接做 MVP 验收
```

## 4. 风险分级

分级规则：

- `P0`：一旦满足部署条件即可导致认证绕过、客户资料泄露或不可接受的数据破坏。
- `P1`：团队试用前必须处理，否则权限、流程或恢复能力不可信。
- `P2`：不会立即阻断受控试用，但会明显增加误操作、维护成本或后续返工。

## 5. P0 / P1 审查发现

### SEC-01：非测试环境可能返回登录验证码

级别：条件性 `P0`，当前整改优先级 `P1`。

现象：

- SMTP 未配置时，验证码接口返回 `dev_code`。
- 当前本地服务实际验证会返回测试验证码。
- 服务当前监听 `127.0.0.1`，但团队试用目标是局域网访问。

风险：

一旦服务开放到局域网而 SMTP 未正确配置，知道已启用邮箱的人可以直接获取验证码并登录。

可直接整改：

- 只有显式 `test/development` 环境允许返回测试验证码。
- 非测试环境缺少强随机认证密钥时拒绝启动。
- 增加生产配置启动检查和回归测试。

需要确认：

- 正式环境名称和配置方式。
- SMTP 系统邮箱和授权方式。
- 是否通过反向代理或 VPN 暴露服务。

### AUTHZ-01：工程师写操作缺少对象归属校验

级别：`P1`。

现象：

当前后端验证用户具有 Engineer 或 PM 角色，但没有统一验证当前工程师是否为任务负责人。受影响入口包括：

- 修改任务。
- 上传任务交付物。
- 提交任务完成说明。
- 提交 Due Date 改期申请。
- 修改或解决风险。

风险：

工程师 A 可以通过构造 API 请求操作工程师 B 的任务。前端隐藏或“我的待办”过滤不能构成授权边界。

可直接整改：

- 建立统一资源授权函数。
- PM 可以管理项目内任务；Engineer 仅能写自己负责的任务。
- 为工程师 A / B 增加每个写入口的 403 回归测试。

确认结论：

- Engineer 只能操作自己账号绑定任务的状态、备注、完成提交、文件上传、改期申请和风险动作。
- 负责人、任务标题、工作包等管理字段只允许 PM 修改。
- 历史未绑定账号的任务暂只允许 PM 写入，完成绑定后再开放对应 Engineer。
- 风险沿用现有闭环：Engineer 可以创建、跟进并提交解决，PM 负责确认关闭、接受或退回。

### FILE-01：系统文件可见度不能替代 NAS ACL

级别：`P1`。

现象：

- API 已按 `engineering`、`pm_only`、`admin_only` 过滤文件记录。
- 直接浏览共享盘时，用户仍可能绕过系统页面和 API。
- 当前 NAS / Windows ACL 尚未按真实目录完成验证。

风险：

工程师如果拥有整个项目目录访问权，仍可能直接查看客户报价、PO 或采购商务资料。

可直接整改：

- 保留并扩充 API 文件可见度回归测试。
- 输出 NAS ACL 测试矩阵和试用验收清单。

确认结论：

- 对客报价、PO 和采购商务对 Engineer 隔离。
- NAS 第一版采用目录级 ACL，并按 Engineer、PM、Admin 用户组验证。
- 当前不设置 Readonly；出现真实管理层只读用户后再评估。
- 文件级例外权限暂不进入当前范围。

### FILE-02：打开文件夹发生在服务器而不是用户电脑

级别：`P1`。

现象：

Web API 使用 `os.startfile()` 在 FastAPI 所在 Windows 主机打开 Explorer。

风险：

- LAN 客户端点击后不会打开自己电脑上的共享目录。
- 普通用户可以反复触发服务器 GUI。
- 接口向客户端返回服务器物理路径。

可直接整改：

- 将服务器端打开行为限制在明确的本机单机模式。
- LAN 模式下不允许 Web 请求启动服务器 GUI。

确认结论：

- LAN 第一版显示并复制 UNC 路径。
- 当前不引入浏览器协议助手或桌面客户端。

### FILE-03：上传和文档解析缺少资源限制

级别：`P1`。

现象：

- 上传文件一次性读入内存。
- 没有文件大小、扩展名、MIME 或请求超时限制。
- PDF、DOCX、XLSX 在请求线程同步解析。
- ZIP/XML 解析没有展开大小、页数和复杂度上限。

风险：

大型模型、超大 PDF 或构造的压缩文档可能耗尽内存、CPU 或磁盘，影响全部用户。

可直接整改：

- 改为流式写入临时文件再原子归档。
- 增加可配置总大小和解析上限。
- 对解析失败保留原文件并记录可排查日志。

确认结论：

- 上传限制配置化，默认单文件上限 500 MB，实施前用真实项目文件校准。
- PDF、DOCX、XLSX 等文档解析使用更小的独立上限。
- 3D 模型等大文件允许归档但不强制解析。
- EXE、脚本、压缩包和未知文件的细分策略在实现时采用安全默认值并保留配置入口。

### DATA-01：一个 INQ 拆多个 WO 的业务口径曾不明确

级别：原 `P1`，2026-07-15 已通过业务约束关闭。

现象：

- 工作台需求允许一个客户询价拆成多个 WO。
- 数据库把 `intake_no` 作为项目级唯一字段。
- 当前没有 Inquiry / Opportunity 父实体或拆单关联字段。

确认结论：

- 当前坚持 `INQ` 与后续 `WO` 一一对应。
- 一份报价包含多台设备时，每台设备分别建立项目、INQ 和 WO。
- 当前不引入 Inquiry / Opportunity 父实体。
- 多设备共用报价或资料通过客户产品/生产线共享层承接，必要时在各项目保留关联文件记录。

重新讨论触发条件：

- 需要按“一份商业机会/报价”汇总多个设备的赢单率、总金额或转化过程。
- 出现一个 INQ 必须转成多个 WO 且不能拆分编号的真实业务案例。
- 报价审批、合同或 PO 必须在多个设备项目之上形成独立生命周期。

### REQ-01：当前需求基线与实际实现不一致

级别：`P1`。

审查时现象：

- 项目资料库 v0.2 将第一阶段描述为单人使用。
- 实际系统已进入 Admin / PM / Engineer 多用户协作。
- 原 MVP 验收要求报价、PO、待办、Excel 导出和数据库/索引/配置备份。
- 代码目前没有报价、PO、待办 CRUD 和 Excel 导出；备份仅覆盖 SQLite 数据库。

风险：

项目无法判断“是否已经完成”，开发优先级会被历史需求持续拉扯。

确认结论：

- 结构化报价记录、PO 记录、独立待办和 Excel/CSV 导出移出当前 MVP，后续独立排期。
- 报价、PO 文件分类、可见度和项目标记继续保留。
- 当前 MVP 的备份最低验收只覆盖 SQLite 数据库；正式备份和恢复参数归入 C-12，团队试用前确认。

### FLOW-01：默认入口和通知不符合行动优先原则

级别：`P1`。

现象：

- 登录后所有角色默认进入全量项目看板。
- 需求要求 PM 优先看到待处理，Engineer 优先看到我的待办。
- 通知表已创建，但没有生成、查询、已读和未读计数闭环。

风险：

系统仍依赖用户记得主动查页面，驳回、改期结果和风险退回可能漏处理。

可直接整改：

- 默认入口按当前用户角色选择，不允许通过查询参数提升角色视角。

确认结论：

- Engineer 默认进入我的待办，PM 默认进入 PM 待处理。
- 多角色用户进入上次使用页面；没有记录时优先 PM 待处理。
- 第一版做系统内通知和未读计数，邮件通知后置。
- 具体通知事件和保留周期在通知模块实现前继续细化，但不再影响总体技术方向。

### OPS-01：局域网部署与恢复能力未闭环

级别：`P1`。

现象：

- 默认启动只监听 `127.0.0.1`，无法直接供团队 LAN 使用。
- 没有 Windows 服务、自启动、反向代理或 TLS 的正式部署脚本。
- 备份 API 只创建数据库备份，没有恢复入口和恢复测试。
- `backup_records` 表没有实际写入。
- 备份机器、目录、周期和恢复责任人仍未确认。

需要确认：

- 正式运行机器、操作系统账号和维护责任人。
- LAN 地址、VPN、反向代理和 TLS 方式。
- 数据库备份目录、频率、保留周期和离线副本。
- 恢复时间目标 RTO、可接受数据丢失 RPO。
- 每月恢复演练由谁执行和记录。

## 6. P2 审查发现

### UI-01：窄屏主导航不可读

390px 宽度下六个主导航仍在同一行，中文被拆成逐字竖排。页面没有横向溢出，但不符合人类快速识别习惯。

建议后续改为可横向滚动导航、两行受控布局或窄屏底部导航。

### UI-02：可访问名称不完整

多个搜索框、工作台动态输入和用户姓名输入依赖 placeholder，没有 label 或 `aria-label`。详情抽屉的焦点进入和 Escape 关闭表现正常。

### UI-03：新建项目缺少渐进录入

新建项目一次展示大量字段。虽然多数不是必填，但用户仍需先理解全部结构。建议拆为：

```text
第一步：客户 + 项目对象
第二步：INQ/WO 和商务日期
第三步：共享层、已有资料包和备注
```

### FLOW-02：模板 Due Date 默认按自然日计算

代码当前使用普通日期加天数。2026-07-15 已确认模板默认按工作日计算，PM 可以人工覆盖；代码仍待整改。

### SEC-02：缺少基础浏览器安全头

当前没有 CSP、`X-Content-Type-Options`、frame 限制和 Referrer Policy，FastAPI `/docs` 与 `/openapi.json` 默认公开。Bearer token 保存在 localStorage，因此 XSS 防护应按高风险处理。

### DEV-01：本地检查完整，但缺少远程 CI

当前没有 `.github/workflows`。`tools/check.cmd` 不包含：

- Python lint 和类型检查。
- JavaScript lint。
- 覆盖率门槛。
- Python 依赖漏洞审计。
- Schema 与运行时迁移漂移检查。
- Playwright E2E。

### DEV-02：数据库迁移缺少版本管理

当前依赖运行时 `ensure_column` 和补充 SQL，没有迁移版本号、升级历史和回滚策略。

### DOC-01：文档存在基线冲突和追加式漂移

主要问题：

- 审查时 `PRODUCT.md` 仍包含 Readonly，权限原则已取消 Readonly；本轮已同步修正。
- 审查时项目需求仍描述单人 MVP；本轮已改为小团队内部试用。
- gap 文档前后同时保留“未实现”和“已实现”。
- roadmap 章节编号顺序已经错乱。
- 运行时创建 `notifications`，基线 SQL 没有该表。
- 缺少需求到代码、测试和验收的追踪矩阵。

## 7. 推荐整改优先级

### 优先级 A：立即处理，不等待业务讨论

这些是安全和工程完整性问题，原则上不需要改变业务方向：

1. 生产环境禁止返回 `dev_code`。
2. 非测试环境强制随机认证密钥。
3. 建立任务对象授权框架和跨账号 403 测试。
4. LAN 模式禁止服务器端 `os.startfile`。
5. 上传改为流式处理，并先增加可配置保护上限。
6. 增加基础安全响应头，正式模式关闭或保护 API 文档。
7. 将上述检查纳入 `tools/check.cmd` 或 CI。

说明：对象授权、上传默认值和 LAN 打开目录方式已经确认，可以按独立提交顺序实施。

### 优先级 B：已确认，可以进入实施

建议按以下顺序实施：

1. Engineer 对象级授权和历史未绑定任务保护。
2. 上传流式处理、大小/类型/解析限制。
3. LAN 复制 UNC 路径，移除远程服务器 Explorer 行为。
4. 角色默认首页。
5. 工作日 Due Date 模板。
6. 系统内通知和未读计数。
7. NAS ACL 试用验证。

正式部署、SMTP、备份、RPO/RTO 和恢复演练责任人仍属于 C-12，团队试用前确认。

2026-07-15 实施进度：

- 第 1 至 6 项已按独立提交完成，并通过后端、权限负向和 Playwright 回归测试。
- NAS ACL 仍需在真实共享目录和 Engineer / PM / Admin 账号下验证，不能由应用层测试替代。
- 通知 v1 已覆盖任务分配、完成/交付提交与审核、改期申请与结果、风险动作；临期/逾期定时提醒和邮件继续后置。

### 优先级 C：完成 P1 后的体验与治理

1. 新建项目渐进录入。
2. 移动端导航和工作台长页面优化。
3. 表单可访问名称、键盘和焦点回归。
4. 需求追踪矩阵。
5. 迁移版本管理、覆盖率和完整 CI。
6. 文档去重、历史归档和章节整理。

## 8. 需要负责人确认的决策清单

| 编号 | 决策 | 2026-07-15 结论 | 状态 |
|---|---|---|---|
| C-01 | 一个 INQ 是否可拆多个 WO | INQ 与 WO 一一对应；多设备分别建项目，不引入父实体 | 已确认 |
| C-02 | 第一阶段是否包含报价/PO/待办/Excel | 移出当前 MVP，后续独立排期 | 已确认 |
| C-03 | Engineer 能编辑哪些任务字段 | 仅自己的状态、备注、完成提交、文件上传、改期申请和风险动作 | 已确认 |
| C-04 | 历史未绑定任务如何授权 | 暂只允许 PM 写，完成账号绑定后再开放 Engineer | 已确认 |
| C-05 | 客户报价/PO 是否对 Engineer 隔离 | 隔离；NAS 使用目录级 ACL | 已确认 |
| C-06 | 是否需要 Readonly | 当前不设；出现真实管理层只读用户后再评估 | 已确认 |
| C-07 | LAN 如何打开文件夹 | 第一版显示并复制 UNC 路径 | 已确认 |
| C-08 | 上传大小和允许类型 | 配置化；默认 500 MB，文档解析另设更小上限 | 已实施，试用期继续采样校准 |
| C-09 | 角色默认首页 | Engineer=我的待办，PM=PM待处理，多角色=上次页面 | 已实施 |
| C-10 | 第一版通知范围 | 系统内通知 + 未读计数，邮件后置 | 已实施 v1 |
| C-11 | Due Date 计算口径 | 工作日；PM 可覆盖 | 已实施；法定节假日/调休表未接入 |
| C-12 | 正式运行与备份参数 | 团队试用前确认 | 未确认、试用阻塞项 |

## 9. 团队试用退出条件

以下条件全部满足后，才建议从单机演示进入小团队 LAN 试用：

- 非测试环境不能返回测试验证码，且 SMTP、认证密钥通过启动检查。
- 工程师不能修改、提交、上传或改期其他工程师的任务。
- 文件上传具备资源上限和失败隔离。
- LAN 用户不能触发服务器 Explorer。
- Engineer / PM / Admin 的 NAS ACL 实测通过。
- 角色默认入口和至少一种待办提醒闭环可用。
- 数据库备份已实际恢复验证一次。
- 明确当前 MVP 验收范围并更新基线需求。
- 所有自动检查通过，关键权限负向用例进入测试套件。
- 正式运行机器、日志、备份、SMTP 和维护责任人有书面记录。

## 10. 后续记录规则

- 每类整改单独提交，不把安全修复、数据模型调整和 UI 改版混为一个 commit。
- 修复安全问题时同步增加负向权限测试。
- 形成业务决策后，更新对应需求、架构计划和本审查文档的确认状态。
- 本文只记录审查和整改入口；实际产品基线仍以完成收口后的 v0.2/v0.3 需求文档为准。
