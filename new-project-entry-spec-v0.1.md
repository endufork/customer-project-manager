# 新项目录入页面规格 v0.1

## 1. 页面目标

新项目录入页是 MVP 的第一入口。目标是让用户在收到新项目资料后，尽快完成：

- 创建项目设备号唯一的项目档案
- 创建标准项目文件夹
- 复制拖入的文件
- 初步分类文件
- 设置项目状态和下一步待办

页面应偏向实际工作流，不做复杂说明页。

## 2. 入口方式

建议提供三个入口：

1. 顶部按钮：新建项目
2. 项目列表页：新建项目
3. 拖入文件或文件夹：自动进入新项目录入流程

拖入文件时，系统先创建一个“待确认导入任务”，用户确认项目设备号后再正式创建项目。

## 3. 推荐流程

```text
Step 1 基础信息
→ Step 2 文件导入
→ Step 3 自动分类确认
→ Step 4 待办和保存
→ 进入项目详情
```

为了降低录入压力，第一版也可以做成单页表单，保存后进入项目详情再补充报价、PO 和更多资料。

## 4. Step 1 基础信息

### 4.1 必填字段

| 字段 | 控件 | 校验 | 说明 |
|---|---|---|---|
| 项目设备号 | 文本输入 | 必填、唯一、英文数字为主 | 公司内部唯一号码 |
| 客户公司 | 下拉选择 + 新建 | 必填 | 可快速新建客户 |
| 项目/设备名称 | 文本输入 | 必填 | 文件夹命名会用到 |
| 当前状态 | 下拉选择 | 必填 | 默认 `询价录入` |
| 币种 | 下拉选择 | 必填 | 默认 `CNY` |

### 4.2 建议字段

| 字段 | 控件 | 校验 | 说明 |
|---|---|---|---|
| 客户联系人 | 下拉选择 + 新建 | 可空 | 若暂不确定可留空 |
| 询价日期 | 日期选择 | 可空 | 默认今天 |
| 预计报价日期 | 日期选择 | 可空 | 可用于自动待办 |
| 预计交期 | 日期选择 | 可空 | 后续执行提醒 |
| 备注 | 多行文本 | 可空 | 记录初始背景 |

### 4.3 项目设备号规则

当前建议规则：

- 必填。
- 不允许与已有项目重复。
- 前后空格自动去除。
- 大小写不敏感。
- 第一阶段建议允许：英文字母、数字、横杠、下划线。
- 暂不建议允许中文和空格。

推荐正则：

```text
^[A-Za-z0-9_-]+$
```

仍需确认：你们公司项目设备号是否会包含横杠、下划线以外的符号。

## 5. Step 2 文件导入

### 5.1 支持导入方式

- 拖入单个文件
- 拖入多个文件
- 拖入文件夹
- 手动选择文件
- 手动选择文件夹

### 5.2 文件处理规则

新项目默认：

- 复制文件到系统项目目录。
- 保留原始文件名。
- 记录原始来源路径。
- 不移动、不删除原始文件。
- 如果目标目录已有同名文件，自动增加序号，例如 `文件名 (2).pdf`。

### 5.3 标准项目目录

保存项目时创建：

```text
客户公司名/
  项目设备号_联系人_设备名称/
    01_询价需求/
    02_方案资料/
    03_内部报价/
    04_客户报价/
    05_PO订单/
    06_图纸模型/
    07_验收发货/
    08_沟通记录/
    99_其他/
```

如果联系人为空：

```text
客户公司名/
  项目设备号_设备名称/
```

如果客户公司暂时未知，理论上可进入 `未分类客户`，但第一阶段建议客户必填，减少后续混乱。

## 6. Step 3 自动分类确认

导入后显示文件列表，让用户确认分类。

### 6.1 文件列表字段

| 字段 | 说明 |
|---|---|
| 文件名 | 当前文件名 |
| 扩展名 | 例如 `.pdf`、`.xlsx`、`.stp` |
| 自动分类 | 系统识别的分类 |
| 分类置信度 | 高/中/低，第一版可不显示 |
| 文件大小 | 便于确认 |
| 是否 3D 模型 | 自动识别 |
| 来源路径 | 原始文件路径 |

### 6.2 分类规则

优先级建议：

1. 3D 模型和图纸后缀优先进入 `图纸模型`。
2. 文件名包含 PO、订单、purchase order 进入 `PO 订单`。
3. 文件名包含报价、quote、quotation 进入 `客户报价`。
4. 文件名包含内部报价、成本、cost 进入 `内部报价`。
5. 文件名包含方案、proposal、solution、spec 进入 `方案资料`。
6. 文件名包含询价、RFQ、需求、requirement 进入 `询价需求`。
7. 无法识别进入 `其他`。

3D 模型后缀建议：

```text
.step .stp .sldprt .sldasm .dwg .dxf .iges .igs .x_t .x_b .prt .asm
```

## 7. Step 4 待办和保存

### 7.1 建议自动待办

根据录入信息自动建议：

| 场景 | 自动建议 |
|---|---|
| 填了预计报价日期 | 创建报价截止待办 |
| 状态为已向客户报价 | 创建客户跟进待办 |
| 填了预计交期 | 创建交期提醒 |
| 文件分类中没有报价文件 | 可提示后续补报价 |
| 文件分类中没有 PO 文件 | 不提示，PO 通常后续才有 |

### 7.2 保存后动作

保存成功后：

- 创建客户和联系人，如为新建。
- 创建项目。
- 创建项目标准目录。
- 复制文件。
- 写入文件索引。
- 对 PDF/Word/Excel/TXT/CSV 提取文本。
- 写入项目事件时间线。
- 进入项目详情页。

## 8. 新项目详情页首屏字段

保存后项目详情页首屏建议展示：

- 项目设备号
- 项目/设备名称
- 客户公司
- 客户联系人
- 当前状态
- 币种
- 询价日期
- 预计报价日期或待办
- 预计交期
- 是否有报价
- 是否有 PO
- 是否有 3D 模型
- 项目文件夹按钮

## 9. 表单默认值

| 字段 | 默认值 |
|---|---|
| 项目来源 | 新项目 |
| 当前状态 | 询价录入 |
| 币种 | CNY |
| 询价日期 | 今天 |
| 是否历史项目 | 否 |
| 是否资料完整 | 否 |
| 是否归档完成 | 否 |

## 10. 错误和提示

### 10.1 必须阻止保存

- 项目设备号为空。
- 项目设备号重复。
- 客户公司为空。
- 项目/设备名称为空。
- 币种为空。
- 项目文件根目录未配置。
- 目标项目目录创建失败。
- 文件复制失败且用户选择“失败即停止”。

### 10.2 可以允许保存但提示

- 未选择联系人。
- 未导入文件。
- 未填写预计报价日期。
- 未填写预计交期。
- 文件分类存在“其他”。
- 文本提取失败。

## 11. 当前缺失信息

需要你后续确认：

1. 项目设备号是否只包含英文字母和数字，还是也会包含横杠、下划线。
2. 项目文件根目录希望放在哪里，例如 `D:\CustomerProjects`。
3. 默认币种是否为 `CNY`。
4. 客户公司是否允许暂时未知。当前建议新项目必须选择客户。
5. 文件导入失败时，是继续保存项目，还是整个保存失败。

